# 角色一键养成项目计划

最后更新：2026-08-09

## 成功标准

在不自动升级角色/武器、不刷摩拉/经验书/元素玉的前提下，用户填写培养目标后，脚本能安全计算缺口，按每周开放日安排已验证培养材料秘境，消耗已有树脂并通过背包复核判断进度。未知来源必须提示而非盲目执行。

## 阶段 0：基础能力（已完成）

- 角色/武器目标、等级/天赋成本、3:1 合成链和背包缺口；设置页提供角色、武器、等级与三项独立天赋的级联选择，自定义文本通过显式开关作为特殊组合和多目标兜底，启用后完全替代上方选择。
- BetterGI 设置页支持按元素选择角色、按类型选择武器和选择常用等级区间，并保留非常规等级/多目标自定义输入；地方特产与怪物路线拥有独立开关。配置按使用频率排列且区块间有分隔线，旧配置保持兼容。
- 首批秘境自动执行：太山府「繁荣」、震雷连山密宫「孤云」。
- 周日奖励序号、切队、神像恢复和树脂优先级。

验收：用户选择培养目标和单次测试模式，显式确认执行后可自动刷两条已验证秘境，且不合成树脂、不领取错误的周日奖励。

## 阶段 1：全培养材料秘境（当前优先）

1. [-] 已建立全部天赋书、武器突破材料的来源 → BetterGI 秘境名称映射表（42 条来源、14 个材料秘境）；待集中实机回归。
2. 对每条映射记录版本、周日组别和至少一次实机验证结果。
3. [x] 正式路径关闭任务级奖励识别；整次运行开始前、全部任务结束后各读取一次背包，以总差值作为最终收益依据。没有背包增长证据时只报告“未确认”，不把任务调用写成成功领奖。
4. 实现按开始时缺口安排多个树脂任务、全部执行后统一复核的循环，直至当次可用树脂耗尽。
5. 基于世界等级 9 固定掉落机制估算世界 Boss 与培养材料秘境的剩余树脂和预计完成天数；路线只使用背包差值样本。

验收：任意只需要秘境培养材料的角色/武器目标，都能在一周开放日循环内自动选择正确秘境，并持续运行到材料满足或资源耗尽。

当前回归进度：周日第 1/2/3 项的计划参数已验证；第 2/3 项实际领奖按用户要求暂缓。已增加只消耗一次原粹树脂的培养秘境单次测试模式，供后续低成本验证。

## 阶段 2：路线材料（后续候选）

1. 调用已订阅的地方特产/怪物路线，并支持文件路径人工覆盖。
2. 分别切换采集队、怪物材料队；确认路线缺失、卡死和战斗失败的停止策略。
3. 用整次运行前后的背包总差值计数采集收益；不在每条 JSON 后读取背包，本次选中的路径全部执行。路线 CD/效率先不纳入核心决策。

验收：有已订阅路线时可执行并统计收益；没有路线时发送清晰的订阅/配置提醒。

当前回归进度：月莲 10 条路线确认获得 49 个；蕈兽共享路线只执行一次并同时统计三阶材料。错误采集队只尝试两次，随后可继续使用另一队伍执行怪物路线，不再按每个 JSON 循环切队。

## 阶段 3：世界 Boss 与周本边界

已完成 BetterGI 内置首领的静态映射与独立队伍配置。世界 Boss 使用 BetterGI 自动首领的“刷至原粹树脂不足”模式且禁用须臾、脆弱树脂；急冻树已完成单次实机回归，验证自动战斗、领奖和背包差值。周本自动执行不进入 0.9.0：保留材料映射用于计算缺口和手动提示，不进入执行队列，也不阻塞其他任务。

预计完成时间中，世界 Boss 按世界等级 9 的 `3.1` 个/40 树脂计算；周本作为手动材料不显示预计天数。

## 阶段 4：圣遗物秘境填充（开发完成，待回归）

- 已实现为当天没有可执行培养树脂任务时的可选任务；用户从 BetterGI 内置圣遗物秘境中选择目标，并配置独立队伍与策略。
- 沿用普通秘境的树脂规则，默认不自动分解圣遗物；待独立回归传送、进本和领奖。
- 圣遗物不显示预计完成天数。
- 圣遗物执行结果不通过目标培养材料差值判断；摘要只说明任务已调用以及圣遗物收益未纳入材料计数，避免错误显示“没有刷取”。

## 近期补充：邮件显示当前养成状态（预研完成，待实现）

- 运行摘要在“本次状态”之后增加“当前养成状态”，明确列出角色当前等级、普通攻击/元素战技/元素爆发当前等级，以及武器当前等级；同时保留目标等级，建议格式为“申鹤 Lv.80→90｜天赋 6/8/8→9/9/9”和“和璞鸢 Lv.80→90”。
- 0.9.x 的手动目标已经包含上述当前值，近期实现直接从解析后的目标生成统一档案快照，不额外打开角色界面，也不增加运行耗时。自定义目标没有填写天赋时显示“天赋未提供（本次不计算）”，不得用 `1/1/1` 冒充当前状态。
- 统一数据结构至少包含目标类型、名称、当前/目标等级、三项天赋当前/目标等级、武器归属、数据来源和读取时间。邮件、日志、`latest-plan.json` 与历史记录共用同一份快照，避免不同输出各自解析后出现差异。
- BetterGI 的 JS 通知正文硬限制为 500 字符。“当前养成状态”属于高优先级区块，应先于任务、收益和缺口写入；多目标超长时只保留完整条目并显示“另 N 项见运行记录”，不能截断半条 HTML。完整目标始终保存在计划与历史记录中，默认不拆成多封邮件，避免一次运行产生多封重复通知。
- 自动档案模式上线后只替换档案快照的数据来源：优先使用通过回归的 `characterDevelopmentTask` 结果。命座带来的天赋 `+3` 必须同时保留界面显示等级和实际培养等级，邮件默认显示实际培养等级，并注明命座加成；一星初始武器仍显示名称和等级，但标记“已忽略培养”。
- 米游社养成计算器可以读取角色等级、天赋和佩戴武器，适合作为开发对照及用户主动刷新的可选快照；登录态约 1–2 周失效、页面结构可能变化且批量选择受限，不适合作为每次 BetterGI 运行和邮件发送的强制前置。

验收：单角色加单武器时，邮件能在 500 字符内完整显示角色当前/目标等级、三项天赋当前/目标等级和武器当前/目标等级；多目标、缺少天赋、命座加成、一星武器及超长摘要均有明确且不误导的降级结果，完整数据可在计划记录中复核。

## 阶段 5：树脂优先级与规则自定义（预研）

- 配置页明确区分“任务类型优先级”和“任务内部使用的树脂类型优先级”。当前固定任务顺序为：世界 Boss → 当天限时培养秘境 → 普通培养秘境 → 圣遗物填充；周本材料只提示手动获取；一次运行只选首个已启用且可执行的树脂任务。正式模式会对该任务依次尽量用完所选树脂，不会在刚好补齐缺口时中途停止；该任务成功或跳过后执行不耗树脂的地方特产/怪物路线，树脂任务失败则停止后续路线。
- 当前世界 Boss 只用原粹树脂；培养材料与圣遗物秘境按浓缩 → 原粹 → 须臾 → 脆弱的 BetterGI 固定顺序，从用户选择的树脂类型中依次消耗；不自动合成浓缩树脂。
- 预研用户可排序的任务类型、原粹树脂保留量、单类任务最大次数/树脂上限、是否允许圣遗物填充，以及须臾/脆弱树脂的单独授权。不可覆盖开放日、材料缺口、执行开关、队伍、已验证映射和周日奖励序号等安全约束。
- 自定义规则只决定“先执行哪个合格任务、执行到什么上限”，不做多个秘境之间的精确比例分配。
- 配置保存前校验重复项、遗漏项、冲突条件和非法数值；后续独立配置预览必须展示解析后的实际执行顺序、各任务上限与跳过原因。

## 阶段 6：自动读取当前档案并生成目标（预开发）

- 配置页普通模式要求用户选择角色、角色目标等级、三项天赋各自的目标等级和自动适配武器的目标等级；不再填写角色当前等级、当前天赋等级、武器名称或武器当前等级。
- 脚本运行开始后，在游戏内定位所选角色并识别当前角色等级、普通攻击/元素战技/元素爆发当前等级，以及当前佩戴武器名称和当前等级；随后与用户选择的四类目标等级组合，转换为现有计算器使用的角色/武器目标。
- 开发基线锁定 BetterGI 最新正式发布版 `0.63.0` 的公开接口。该版本向 JS 注入 `characterDevelopmentTask`，提供 `GetCharacter` 和 `GetMultiCharacters`，可按“属性;武器;天赋”读取角色等级/上限、武器名称与等级/上限、三项天赋显示等级及命座加成标记，不再自行重复实现角色页导航和 OCR。
- 先做能力检测：接口存在时允许自动档案模式，接口不存在时保留 0.62.0 的完整手动输入；自动模式下接口缺失或返回字段不完整必须明确拒绝，不能静默用默认值。项目最低 BetterGI 版本暂不提高，自动档案功能单独标注需要 `0.63.0+`。
- 代码阶段使用模拟接口完成适配器、目标转换、配置界面和单元测试，不升级当前主力 BetterGI，也不运行游戏识别。开始实机回归前，再提醒用户说明此前实际尝试过的识别方案和结果，并将其作为测试基线。
- 米游社养成计算器能够提供角色等级、天赋和佩戴武器数据，可作为开发阶段的对照数据及可选本地快照来源；第一版不把浏览器登录态作为正式运行的强制依赖。
- 装备为一星初始武器时自动忽略武器培养；规则按静态数据中的 `rarity=1` 判断，对应无锋剑、训练大剑、新手长枪、学徒笔记和猎弓。
- 识别失败、角色不存在、武器未知或数据不完整时停止生成该目标并发送提醒，禁止按 1 级或默认武器盲目计算。

验收：用户选择一个已拥有角色及角色/三项天赋/武器的目标等级后，脚本能在不手填当前养成数据和武器名称的情况下生成与游戏界面一致的目标；普通武器自动加入，初始一星武器自动忽略。自动档案模式必须先展示识别原始值、用户选择的目标值和转换后的最终目标，用户确认后才能实际调度。

## 阶段 7：可选扩展
- 来源级队伍和战斗策略覆盖。
- 更易用的可视化目标编辑与历史统计展示。

## 每阶段共同验收原则

- 先完成本地计算与配置校验，再用单次测试进行小树脂实机验证，最后允许连续运行。
- 失败、未知和未确认收益必须显式呈现，不能默认为成功。
- 每次功能上线同步更新 `开发TODO.md`、`技术方案.md`、`项目计划.md` 与 README。

## 连续开发与回归约定

本项目采用“先持续开发、后集中实机回归”的节奏：不再因为等待游戏内测试而暂停实现。对无法在本地自动验证的 BetterGI 调用、OCR 结果、传送路径和战斗结果，开发阶段默认接口行为符合官方定义，并在 TODO 中保留风险和回归步骤。

集中实测时按功能逐项回归；测试结果与假设不符时，以实测为准修正实现、映射或策略，再重新执行本地测试。未经实测的功能不会在文档中标记为“已验证”。
