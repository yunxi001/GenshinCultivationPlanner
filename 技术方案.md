# 角色一键养成技术方案

最后更新：2026-08-10

## 1. 目标与非目标

目标是：用户在 BetterGI 配置页填写角色/武器培养目标后，脚本计算缺口、读取背包、制定当天/每周刷取策略，并自动执行已验证的来源任务，直到所需材料完成或当次资源耗尽。

第一阶段不做角色或武器的游戏内升级；不自动刷摩拉、经验书、元素突破玉；不合成浓缩树脂。已适配世界 Boss 可执行，路线执行依赖用户已订阅路径；周本材料只计算缺口并提示手动获取。

## 2. 总体架构

```text
手动档案 / BetterGI 自动档案
          │
          ▼
目标解析与校验 ──► 规则库（genshin-db 生成）
          │                    │
          ▼                    ▼
背包读取 ───────────────► 需求、合成链、展示缺口
                                      │
                                      ▼
                         来源映射与周循环调度
                                      │
                         ┌────────────┴────────────┐
                         ▼                         ▼
                    BetterGI 秘境执行器       路线/Boss 执行器
                         │
                         ▼
             全部任务结束后统一背包复核、运行记录、邮件摘要
```

### 模块边界

- `core/target-input.js`、`core/profile.js`：手动目标解析，以及计划、历史、日志和邮件共用的档案快照。
- `core/character-development.js`：检测 BetterGI 0.63.0 角色养成接口，归一化角色、天赋和佩戴武器，处理命座 `+3` 与一星武器。
- `core/settings.js`：把角色/武器与等级下拉、地方特产/怪物路线独立开关、级联运行模式和合并高级输入转换为执行器已有字段，并兼容升级前保存的旧目标文本和路线总开关。
- `core/requirements.js`、`calculator.js`、`crafting.js`、`display-shortages.js`：材料需求、3:1 合成折算、实际展示缺口。
- `core/inventory.js`：按背包页读取目标和合成链材料。
- `data/rulebook.json`、`materials.json`、`crafting-recipes.json`：由 `tools/build-rulebook.mjs` 生成的游戏静态数据。
- `data/source-execution-map.json`：来源关卡到 BetterGI 可执行名称的映射；`data/domain-validation-ledger.json` 单独保存 42 条映射的验证状态、版本、开放日、周日序号和结果。
- `core/scheduler.js`、`core/execution-policy.js`：按开放日合并候选，并应用用户配置的任务顺序、次数上限和原粹树脂预算。
- `core/execution-result.js`：统一 `completed`、`skipped`、`failed`、`unconfirmed` 四种结果及后续任务停止规则。
- `core/domain-executor.js` 与 `main.js`：构造 `AutoDomainParam`、切队、神像恢复、执行秘境、复核库存。
- `core/route-executor.js`：默认关闭；对已订阅路线按类型切换队伍，通过 `pathingScript.RunFileFromUser()` 从 `User/AutoPathing` 执行。整次运行开始前和全部任务结束后各读取一次背包，以总差值回填路线收益，不在每条 JSON 后重复读取。执行前通过 `dispatcher.AddTrigger(new RealtimeTimer('AutoPick'))` 补齐调度器原生 `Pathing` 项目自动启用的拾取触发器，并继承当前自动培养配置组的地图追踪/自动战斗参数。BetterGI 地图追踪接口不返回可靠的成功状态，因此最终背包无库存增长时标记“未确认”，不得写成成功。
- `core/route-estimate.js`：只使用相同 JSON 文件集合的历史背包正增长，估算路线剩余轮次；样本不足、路径变化和零收益不输出结论。
- `core/report.js`、`core/history.js`：生成 500 字符内的邮件摘要和最多 100 条运行历史。档案按完整条目截断，完整内容保存在计划和历史中。

## 3. 数据与映射策略

genshin-db 可以给出材料的来源关卡、开放日、合成配方及角色/武器培养成本，但 BetterGI 的 `DomainName` 是游戏地图中的秘境名称，不能假定与来源关卡名称完全相同。因此：

1. 先由生成器产生候选来源；
2. 再在 `source-execution-map.json` 写入与 BetterGI 内置目录交叉核对的名称；
3. `domain-validation-ledger.json` 记录每条映射是待验证、目录核对、历史通过、验证通过或失败；
4. 无映射内容显示为人工待办。存在映射但未实测的内容会保留“待验证”记录，发布说明必须如实披露。

`data/bettergi-domain-catalog.json` 保存从 BetterGI 本体提取的材料秘境目录；规则库生成器会校验每个映射名称在目录中存在，防止拼写错误或错误传送到不相关秘境。

开放日默认按 BetterGI 服务器时区和游戏每日 04:00 刷新点计算；手动星期仅作高级兜底。周日按开放日自动派生奖励序号：周一/周四材料为 `1`，周二/周五为 `2`，周三/周六为 `3`。遇到例外可在映射文件显式覆盖。

## 4. 调度与资源规则

默认树脂任务顺序是世界 Boss → 当日限时培养秘境 → 普通培养秘境 → 圣遗物填充。用户可以重排四类任务；未启用项和上限为零的任务会跳过，配置、切队、传送或执行异常会停止后续树脂任务。地方特产与怪物路线可以放在树脂任务前或后，路线局部失败只影响对应路线。Boss 默认关闭，只使用原粹树脂；周本材料不进入自动队列。

计划同时生成“今天队列”和“从今天起七天循环策略”。循环策略用于提示每个开放日能刷什么；执行器仍只会执行当天队列，避免跨日或未开放内容被错误启动。

秘境树脂规则：不合成浓缩树脂。BetterGI 的实际顺序固定为浓缩 → 原粹 → 须臾 → 脆弱，脚本只提供该顺序的安全子集。须臾与脆弱分别授权，默认关闭。本次原粹树脂预算是用户填写的上限，不是游戏内读取值；保留量从预算中扣除，再按任务顺序保守预留。秘境次数上限分别作用于每种已授权树脂，Boss 上限是运行次数。全部任务结束后统一复核背包，不在任务之间读取。

培养秘境、世界 Boss 与圣遗物秘境分别提供单次测试模式。培养秘境单次测试强制只允许 1 次原粹树脂领奖，并把浓缩、须臾、脆弱树脂上限置零，避免验证周日奖励序号时耗尽树脂。

路线 CD/效率不作为第一阶段决策依据，以免因路线漏采、卡死而产生错误完成判断；后续若背包差值统计稳定，再评估加入。

## 5. 可靠性与安全边界

- 目标文本校验失败、缺少队伍、周日奖励序号缺失时，拒绝执行。
- 切队、传送、战斗、秘境失败应记录原因，不把失败伪装成材料已获得。
- 正式执行关闭秘境、Boss 和圣遗物的任务级奖励识别；最终培养材料收益只依赖整次运行开始前、全部任务结束后的背包总差值确认。圣遗物不属于目标培养材料，不把目标材料差值为零写成圣遗物执行失败。
- 配置页提供顶层执行确认复选框，默认不勾选；未确认时在读取背包、写入记录、发送通知或操作游戏前立即终止。
- 任何未知来源、未订阅路线、未适配 Boss 都提示用户，不自动尝试。
- 切换同一队伍最多尝试两次：直接切换失败后传送神像重试一次；再次失败则在本次运行中标记该队伍不可用，跳过其余相关路线。一个材料的多条 JSON 路径视为同一个路线任务；为避免频繁打开背包，本次选中的路径全部执行后才统一核对，因此不能途中按缺口提前停止，可能少量多刷。路线初始化或收尾异常必须转为失败记录，不能阻止 `latest-plan.json`、历史和通知的保存。
- 路线发现成功后，会在本次计划中把对应材料动态标记为 `executionType=route` 并移出人工待办。相同路线至少积累两个正收益样本后可估算剩余轮次，但不加入刷新周期，也不换算自然日。

BetterGI 的 `SettingItem` 支持输入、普通选择、复选、级联选择和分隔线，但不支持按父开关动态隐藏。配置页因此同时保留手动档案和自动档案字段，并在标签中写明适用模式。角色名称按元素分组；手动模式选择当前/目标区间，自动模式只选择目标等级。启用自定义手动目标后，文本会完全替代上方手动选择，避免两套输入叠加。地方特产与怪物材料各有独立区块、开关和队伍；Boss 可以另填专属覆盖。高级设置集中放置任务顺序、树脂预算/保留量、每类上限、路线时机和消耗品树脂授权，运行摘要开关仍在最底部。`core/settings.js` 在运行前归一化新旧字段，旧路线总开关和旧树脂组合继续兼容。

## 6. 当前实现与待回归内容

代码已经包含材料计算、手动/自动档案、背包读取、合成链、42 条秘境来源映射、多树脂任务调度、四状态结果、路线/Boss 专属覆盖、树脂规则预览、邮件和历史记录。另有 37 项周本材料映射用于手动提示，以及 39 项世界 Boss 材料的 BetterGI 内置任务映射。急冻树和少量路线已有历史实测；自动档案、多树脂连续调用、其余秘境映射、Boss、圣遗物和新增规则仍待集中实机回归。

完成预估按用户确认的世界等级 9 固定机制计算：世界 Boss 为 `3.1` 个角色突破材料/40 树脂，培养材料秘境使用最高难度掉落期望；每日树脂预算默认 180。急冻树的计算用例已验证：极寒之核缺口 15 对应 5 次领奖、约 200 树脂。摘要同时显示预计领奖次数、树脂、实际需要的开放日数量，以及从运行当天起最早经过的自然日；因此连续两天看到的“自然日”可能随开放日距离变化，并不代表材料缺口增加。周本与圣遗物不显示预计天数，路线仅在有对应路径的背包前后差值样本后预估，不考虑双倍掉落。

## 7. 树脂优先级与执行规则

自定义配置分成两层：任务类型优先级决定先选择世界 Boss、限时培养秘境、普通培养秘境或圣遗物填充；树脂类型优先级决定培养材料/圣遗物秘境内部允许消耗哪些树脂。地方特产和怪物材料路线不消耗树脂，单独配置在树脂任务前或后，不能混入树脂额度计算。

当前规则支持原粹树脂保留量、每类任务次数上限、圣遗物填充条件、路线前后顺序，以及须臾/脆弱树脂的独立授权。次数是授权上限，不能宣称精确停在材料刚好满足的时刻。开放日、真实缺口、执行确认、必填队伍、周日奖励序号和 Boss 只使用原粹树脂属于硬约束。`tools/preview-execution-policy.mjs` 可在不启动游戏时打印最终规则；冲突配置会直接报错。

## 8. 角色档案自动识别

自动档案模式要求用户选择角色、角色目标等级和三项天赋目标。武器有三种模式：自动读取当前佩戴武器、不培养武器、手动指定独立武器目标。前两类角色当前值及自动佩戴武器由 BetterGI 0.63.0 的 `characterDevelopmentTask.GetCharacter/GetMultiCharacters` 读取；手动武器使用用户填写的“武器名:当前>目标”，不建立角色佩戴关系。归一化结果进入同一计算管线，0.62.0 继续使用手动档案。

等级对象使用 `currentAscended` 和 `targetAscended` 区分 20/40/50/60/70/80 级的突破前后。手动下拉仅为当前等级显示突破前后，目标等级默认完成对应突破，每级只显示一个选项；自定义目标仍支持 `70前/70后` 简写和显式的目标突破前状态。未标注时当前按突破前、目标按突破后解释。自动档案根据 `LevelLimit/WeaponLevelLimit` 是否高于当前等级判断当前状态；临界等级缺少有效上限时立即报错。突破材料只统计在当前状态之后且目标状态之前（含目标选择“突破后”）的档位，档案邮件沿用同一状态文本。

截至 2026-08-09，最新正式发布版为 `0.63.0`。源码确认 JS 全局对象 `characterDevelopmentTask` 提供 `GetCharacter(string, categories)` 和 `GetMultiCharacters(JS Array, categories)`，分类字符串为“属性;武器;天赋”；返回 `CharacterName`、`Level/LevelLimit`、`WeaponName/WeaponLevel/WeaponLevelLimit`、`Attack/Skill/BurstLevel` 与对应 `HasBonus`。第一版直接以这一公开契约编写适配器，不再复刻角色筛选、导航和 OCR。运行前采用能力检测而非只比较版本字符串：对象和所需方法存在才允许自动模式，否则继续支持手动模式；自动模式选择后接口缺失则拒绝运行。项目最低版本暂保留 0.62.0，自动模式单独要求 0.63.0+。

0.63.0 同时进行了大规模识图资源重构，官方明确标记当前版本不稳定。接口归一化、命座等级转换、目标生成和配置界面已完成模拟测试；主力环境暂不升级，游戏识别留到后续。米游社养成计算器只作开发对照，不是运行依赖。

自动识别和手动目标最终都生成 `profileSnapshot`。武器名称必须存在于规则库；`rarity=1` 时保留档案显示，但跳过武器培养。关键字段为空会在背包读取和调度前终止。命座 `+3` 同时保存界面等级和实际培养等级，材料计算只使用实际等级。

## 9. 邮件中的当前养成状态

0.9.0 的目标对象已经包含角色/武器的 `level.current`，角色天赋目标还包含 `normal/skill/burst.current`，因此近期邮件增强不依赖游戏界面识别。目标解析完成后新增统一档案快照，例如 `{ kind, name, level: { current, target }, talents, equippedBy, source, scannedAt }`；手动目标的 `source` 为 `manual-settings`，自动档案以后使用 `bettergi-character-development`，米游社 JSON 只作为开发对照或用户主动刷新的 `miyoushe-snapshot`。邮件、日志、`latest-plan.json` 和 `history.json` 只消费这一份快照。

邮件在“本次状态”后增加高优先级的“当前养成状态”。角色格式为“角色名 当前等级（突破状态）→目标等级（突破状态）｜天赋 普攻/战技/爆发 当前→目标”，武器使用相同的等级状态写法。手动自定义目标没有天赋字段时显示“天赋未提供（本次不计算）”，不能补成默认等级；当前独立武器目标也不能声称它一定由某个角色佩戴。自动档案数据若带命座天赋 `+3`，快照同时保存界面值和扣除加成后的实际培养值，计算和邮件主值均使用实际培养值，并附“含命座加成”提示。一星武器仍进入摘要，但标记为忽略培养。

BetterGI 的 `notification.Send()` 会拒绝超过 500 字符的消息，且一分钟最多 5 次调用。默认方案保持每次运行只发一封邮件：按“标题/状态/当前养成状态/失败原因/任务与收益/缺口与策略”的优先级分配字符预算；多目标时只追加能够完整放入的条目，并用“另 N 项见运行记录”收尾，不允许截断 HTML 标签或半条目标。完整快照始终写入计划和历史文件。米游社计算器虽然可以取得角色等级、三项天赋和佩戴武器，但登录态通常需要定期刷新，页面结构与交互引用也可能变化，因此不放入每次 BetterGI 运行的必经链路。
