# 角色一键养成技术方案

最后更新：2026-08-20

## 1. 目标与非目标

目标是：用户在 BetterGI 配置页填写角色/武器培养目标后，脚本计算缺口、读取背包、制定当天/每周刷取策略，并自动执行已验证的来源任务，直到所需材料完成或当次资源耗尽。

第一阶段不做角色或武器的游戏内升级；不自动刷摩拉、经验书、元素突破玉；不合成浓缩树脂。已适配世界 Boss 可执行，路线执行依赖用户已订阅路径；周本材料只计算缺口并提示手动获取。

## 2. 总体架构

```text
手动目标 / 自动角色档案 / 高级 JSON
          │
          ▼
目标解析与校验 ──► 规则库（genshin-db 生成）
          │                    │
          ▼                    ▼
背包读取 ───────────────► 需求、合成链、展示缺口
                                      │
                                      ▼
                         来源映射与周循环调度
                                      │
                         ┌────────────┴────────────┐
                         ▼                         ▼
                    BetterGI 秘境执行器       路线/Boss 执行器
                         │
                         ▼
             全部任务结束后统一背包复核、运行记录、邮件摘要
```

### 模块边界

- `core/target-input.js`：简洁文本目标解析与防错校验。
- `core/character-profile.js`：调用 BetterGI 角色养成识别接口，规范化角色、天赋和武器当前状态，并转换为现有标准目标；负责突破前后、命座加成和一星武器边界。
- `core/settings.js`：把自动档案目标、手动指定武器、路线独立开关、运行模式和紧凑 Boss 专属配置转换为执行器字段，并兼容升级前保存的结构化 Boss 配置槽和路线总开关。
- `core/requirements.js`、`calculator.js`、`crafting.js`、`display-shortages.js`：材料需求、3:1 合成折算、实际展示缺口。合成折算只采用确定的基础配方，不预估角色天赋带来的概率返还或额外产出；额外收益在后续背包复核时按实物计入。
- `core/inventory.js`：按背包页读取目标和合成链材料。
- `data/rulebook.json`、`materials.json`、`crafting-recipes.json`：由 `tools/build-rulebook.mjs` 生成的游戏静态数据。
- `data/source-execution-map.json`：人工验证的“来源关卡 → BetterGI 可执行名称”映射；这是自动执行的安全边界。
- `core/scheduler.js`：按开放日、来源类型、优先级合并和排序任务。
- `core/domain-executor.js` 与 `main.js`：构造 `AutoDomainParam`、切队、神像恢复、执行秘境、复核库存。
- `core/route-executor.js`：默认关闭；对已订阅路线按类型切换队伍，通过 `pathingScript.RunFileFromUser()` 从 `User/AutoPathing` 执行。整次运行开始前和全部任务结束后各读取一次背包，以总差值回填路线收益，不在每条 JSON 后重复读取。执行前通过 `dispatcher.AddTrigger(new RealtimeTimer('AutoPick'))` 补齐调度器原生 `Pathing` 项目自动启用的拾取触发器，并继承当前自动培养配置组的地图追踪/自动战斗参数。BetterGI 地图追踪接口不返回可靠的成功状态，因此最终背包无库存增长时标记“未确认”，不得写成成功。
- `core/rewards.js`：解析 ClearScript 返回的任务奖励字典，并按材料合并背包差值与任务奖励。背包结果可信时优先使用；背包漏识别时才用任务奖励兜底，不重复累计。
- `core/report.js`、`core/history.js`：生成邮件摘要和最多 100 条运行历史，同时保存任务识别、背包差值、最终采用来源及冲突记录。

## 3. 数据与映射策略

genshin-db 可以给出材料的来源关卡、开放日、合成配方及角色/武器培养成本，但 BetterGI 的 `DomainName` 是游戏地图中的秘境名称，不能假定与来源关卡名称完全相同。因此：

1. 先由生成器产生候选来源；
2. 再在 `source-execution-map.json` 人工写入并实测 BetterGI 名称；
3. 只有已验证条目状态为 `supported`，才能进入执行队列；
4. 无映射或失败映射显示为人工待办，绝不自动猜测和进入秘境。

`data/bettergi-domain-catalog.json` 保存从 BetterGI 本体提取的材料秘境目录；规则库生成器会校验每个映射名称在目录中存在，防止拼写错误或错误传送到不相关秘境。

开放日默认按 BetterGI 服务器时区和游戏每日 04:00 刷新点计算；手动星期仅作高级兜底。周日按开放日自动派生奖励序号：周一/周四材料为 `1`，周二/周五为 `2`，周三/周六为 `3`。遇到例外可在映射文件显式覆盖。

## 4. 调度与资源规则

兼容模式的树脂任务排序是世界 Boss → 当日限时培养秘境 → 普通培养秘境 → 圣遗物填充，并且仍只选择首个已启用任务。自定义模式由树脂规则 V2 生成最多四项的有界队列，每个类别只选择排序最前的一个具体 Boss/秘境；这样可以连续执行不同类别任务，同时明确不在多个同类秘境之间做精确分配。整条队列会在任何自动任务开始前编译执行配置，后续项缺队伍、周日序号或合法树脂时会提前拒绝运行。

每个队列项最多调用一次 BetterGI 接口。任务失败、奖励未确认或明确树脂不足时停止后续树脂任务；已完成时继续下一项。地方特产与怪物材料路线不耗树脂，自定义模式可放在树脂队列前或后。Boss 属于高策略复杂度任务，默认不自动执行；仅在用户确认机制、专属队伍与战斗策略后显式开启，且只使用原粹树脂。周本材料不进入自动队列，也不会阻塞 Boss、秘境或路线。

计划同时生成“今天队列”和“从今天起七天循环策略”。循环策略用于提示每个开放日能刷什么；执行器仍只会执行当天队列，避免跨日或未开放内容被错误启动。

秘境树脂规则：不合成浓缩树脂。脚本使用 BetterGI 0.64 的 `ResinPriorityList` 显式传入所选顺序；默认只启用浓缩和原粹，须臾与脆弱必须显式授权。BetterGI 的四个次数是分别累计而不是总次数，因此有限总预算只交给顺序中的第一种已授权树脂，避免“每种树脂各刷 N 次”突破用户上限；无限预算才使用完整组合。每次运行按开始时库存和缺口生成队列，任务之间不打开背包，全部任务结束后统一复核。

培养秘境、世界 Boss 与圣遗物秘境分别提供单次测试模式。培养秘境单次测试强制只允许 1 次原粹树脂领奖，并把浓缩、须臾、脆弱树脂上限置零，避免验证周日奖励序号时耗尽树脂。

路线 CD/效率不作为第一阶段决策依据，以免因路线漏采、卡死而产生错误完成判断；后续若背包差值统计稳定，再评估加入。

## 5. 可靠性与安全边界

- 目标文本校验失败、缺少队伍、周日奖励序号缺失时，拒绝执行。
- 切队、传送、战斗、秘境失败应记录原因，不把失败伪装成材料已获得。
- 正式执行默认开启秘境、Boss 和圣遗物的任务级奖励识别。最终培养材料收益逐材料选择证据：背包差值可信时优先；结束背包图标或数量识别失败时使用任务奖励兜底；冲突时采用背包并记录双方结果。圣遗物不属于目标培养材料，不把目标材料差值为零写成圣遗物执行失败。
- 背包物品名称使用 BetterGI 0.64 的 `ItemIconRecognitionMode.Item`（ItemV2）；图标未匹配与数量 OCR 失败分别记录，只有数量文字问题提示检查 OCR V6。项目不再兼容 0.64.0 之前的 BetterGI。
- 执行前 ItemV2 未找到材料时按零处理。若历史最近一次已确认该材料为正数，则记录“可能已消耗或漏识别”的风险提示，但仍按零继续计算和刷取，避免一次识别冲突导致后续运行永久停摆；该策略可能重复获取材料，用户使用材料升级后应及时更新当前等级。
- 配置页顶部提供执行确认复选框，默认不勾选；未确认时在读取背包、写入记录、发送通知或操作游戏前立即终止。单次模式仅用于用户自愿验证，不作为首次运行要求。
- 任何未知来源、未订阅路线、未适配 Boss 都提示用户，不自动尝试。
- 切换同一队伍最多尝试两次：直接切换失败后传送神像重试一次；再次失败则在本次运行中标记该队伍不可用，跳过其余相关路线。一个材料的多条 JSON 路径视为同一个路线任务；为避免频繁打开背包，本次选中的路径全部执行后才统一核对，因此不能途中按缺口提前停止，可能少量多刷。路线初始化或收尾异常必须转为失败记录，不能阻止 `latest-plan.json`、历史和通知的保存。
- 自动首领保持 `ReturnToStatueAfterEachRound=false`，避免每轮领奖都额外传送；整个首领任务返回或抛出异常后，由脚本统一尝试传送一次七天神像。这样可处理 BetterGI 在最后一次领奖后先刷新 Boss、再发现树脂不足并直接返回的情况，避免把战斗或阵亡状态留给后续背包复核与一条龙任务。退场异常只记录警告，不覆盖原始执行结果。
- 执行状态统一为 `completed/unconfirmed/skipped/failed`，并保存稳定的 `code`、执行 `stage`、`severity`、`retryable`、证据和时间。调用位置负责标记配置、背包、切队、任务、奖励或清理阶段；只有 BetterGI 明确写出树脂不足时才归类为 `no_resin`，奖励为空本身只表示未确认。新结构保留旧 `execution.task/status/reason`，历史文件无需迁移重写。
- 路线发现成功后，会在本次计划中把对应材料动态标记为 `executionType=route` 并移出人工待办；这不会把路线加入树脂周计划。路线完成天数仍待接入历史背包差值。

BetterGI 0.64 的 `SettingItem` 支持输入、选择、复选、级联选择和分隔线，但不支持按父开关动态隐藏子项。公开设置只保留自动档案流程；“培养内容”提供角色和当前佩戴武器、角色和指定武器、仅角色、仅指定武器四种组合。角色、目标等级和天赋使用下拉；指定武器继续使用武器下拉与当前→目标区间。普通 Boss 由材料缺口自动匹配；少量机制特殊的 Boss 使用一个开关和一个文本框配置 `Boss:队伍=名称,策略=名称` 或 `Boss:禁用`，并在操作游戏前与 BetterGI Boss 目录校验。旧版 `Boss=队伍|策略` 仅作为已保存配置的兼容格式。

## 6. 当前实现与缺口

已具备计算、背包读取、合成链、输入校验、48 条来源到秘境映射、周日选择、秘境执行、邮件和历史记录；另有周本材料映射用于手动提示，以及世界 Boss 材料映射。Boss 下拉列表以当前支持基线 BetterGI 0.64.0 的自动首领目录为准，不会因静态游戏数据库先更新就调用尚未支持的新 Boss。圣遗物秘境填充已进入 0.9.0，并在 2026-08-06、08-07、08-09 使用“月童的库藏”完成实机验证。世界 Boss 中，急冻树、隐山猊兽和霜夜巡天灵主已确认战斗、领奖及材料增长；守望者·堕天和蕴光月幻蝶仅确认任务调用，其他 Boss 尚未逐项回归。

规则库已更新至 `genshin-db 5.2.14`，包含沃雅妮莎与薇斯纳的正式突破、天赋数据。两人的新特产与怪物材料由订阅路线发现；天赋书沿现有荒坠的圣迹映射执行。Boss 适配仍受 BetterGI 最低兼容版本约束：不灭衍生造物从 0.65.0 才进入自动首领名单，游雪的护刃在 0.65.0 尚不支持，两项材料当前都给出手动获取提示。上游此次也将星锋剑突破成本改为零，生成器按该版本原始数据处理。

完成预估按用户确认的世界等级 9 固定机制计算：世界 Boss 为 `3.1` 个角色突破材料/40 树脂，培养材料秘境使用最高难度掉落期望；每日树脂预算默认 180。急冻树的计算用例已验证：极寒之核缺口 15 对应 5 次领奖、约 200 树脂。世界 Boss 显示预计领奖次数、树脂和按每日预算所需天数；培养秘境显示实际需要的开放日数量及从运行当天起的最早自然日。周本与圣遗物不显示预计天数，但周本缺口不会遮挡同一计划中 Boss/秘境的估算；路线仅在有对应路径的背包前后差值样本后预估，不考虑双倍掉落。

## 7. 树脂优先级与执行规则自定义

树脂规则 V2 分为任务队列和秘境树脂类型。默认“一次只刷一类（推荐）”保留旧行为；选择“按顺序刷多类”后，四个级联槽分别同时选择任务类别与领奖次数。“用完可用树脂”表示当前类别不设上限并终止后续树脂队列；旧版分开的任务顺序、上限字段及“全部”值继续兼容。地方特产和怪物材料路线不消耗树脂，只能选择在树脂任务前或后执行。

`core/resin-policy-v2.js` 将设置编译为冻结的 `schemaVersion=2` 策略，并记录到最新计划和历史。重复/遗漏任务类型、未知值、零次、负数、小数或超过安全范围的上限会在背包读取和游戏操作前报错。`pnpm validate:resin` 可以脱离游戏输出与运行时完全相同的规范化策略。须臾和脆弱树脂仍需显式授权；Boss 仍只使用原粹树脂。BetterGI 没有可靠的 JS 剩余树脂查询接口，因此不提供名不副实的“精确保留原粹树脂”设置。

V2 模型已接入连续队列：兼容模式保持一次只执行首个任务，自定义模式最多连续调用四个不同类别的任务。仍只读取运行前后两次背包，不在每次领奖后复查，也不进行多个同类秘境间的精确比例分配。领取上限是运行预算，不代表材料一定刚好补齐；开放日、真实缺口、执行确认、必填队伍、已验证来源映射和周日奖励序号仍是不可覆盖的硬约束。

## 8. 角色档案自动识别（代码已完成，待实机验证）

自动档案模式由用户选择培养内容、角色目标等级和三项天赋目标等级；脚本按需通过 BetterGI 0.64 的 `characterDevelopmentTask.GetCharacter()` 读取角色、武器和天赋信息。仅培养指定武器时直接使用用户选择的武器及当前→目标区间，不打开角色档案。公开设置提供“预览目标”和“读取档案并执行”两种方式，旧武器处理选项会迁移到对应的新培养内容。仅预览保存 `latest-profile.json` 后结束，不读取背包、不通知、不调度。目标培养上限仍由用户决定，脚本不自动推荐。该功能已纳入当前 0.9.0 发布内容。

当前维护版本的最低版本为 BetterGI 0.64.0，并要求启用 OCR V6；更早版本和其他 OCR 实现不作为兼容目标。脚本仍会检测 `GetCharacter`、自动秘境树脂优先级等实际能力，不能只相信版本号；接口缺失时在操作游戏前停止。脚本无法查询或切换 OCR 版本；角色、武器或天赋文字异常时提示确认 OCR V6。

规范化档案同时保存 `{ level, levelLimit }`，用等级上限区分突破前后；目标也使用相同结构。命座加成标记为真时从界面天赋等级扣除 3，再校验实际培养等级范围。武器名称须在规则库存在；`rarity=1` 时跳过武器目标。当前进度达到或超过目标时标记完成并跳过；单个字段无法确认时只标记对应目标失败，其他独立目标继续。所有所选目标都无法计算时才终止，不能回退为 1 级。目标状态与生成结果一并写入最新档案和历史，邮件显示角色、天赋、武器的当前状态与目标。

OCR V6 专项测试此前验证了 25 名角色首轮、11 名重点角色重复读取及五种武器筛选类型，但当时武器和天赋字段为空，只证明定位、筛选和角色等级链路。当前适配器与主流程已通过本地模拟；完整字段识别、命座加成和自动目标到实际调度的端到端行为仍标记为待实机验证。
