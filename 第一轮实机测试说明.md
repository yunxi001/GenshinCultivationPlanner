# 第一轮实机测试说明

本轮只验证三个执行器：周本、世界 Boss、圣遗物秘境。每次只开一种，避免调度优先级与树脂消耗互相干扰。

## 开始前共同设置

1. 确认 BetterGI 已处于游戏内可操作状态，队伍名称与配置中填写的文字完全一致。
2. 首次先勾选“仅生成计划”，运行并查看日志；确认候选任务正确后，再取消勾选进行实际执行。
3. 本轮关闭“执行已匹配路线”和“圣遗物秘境填充”（仅在第三项测试时开启）。
4. 须臾树脂、脆弱树脂保持关闭；不要在树脂不足时测试。
5. 每次实际执行结束后，保存 BetterGI 日志和 `record/latest-plan.json`；不要立即重复运行，以免消耗额外树脂。

## 测试 0：计划模式安全检查（不消耗树脂）

保持“仅生成计划”开启，并填写周本队伍、Boss 队伍和圣遗物队伍名称。

目标文本可先使用：

```text
香菱:80>80,6/6/6>7/6/6
```

预期：日志出现周本候选，类型为 `weeklyBoss`，目标为“深入风龙废墟”；不切队、不传送、不消耗树脂。

若候选类型、材料或目标不正确，停止后保存完整日志，不进行下一步。

## 测试 1：周本执行（优先进行）

1. 保持上述香菱天赋目标；关闭圣遗物填充和路线执行。
2. 填写周本队伍；也可只填写 Boss 队伍作为周本队伍兜底。
3. 确认队伍可击败风魔龙，且有至少一次周本领奖机会与足够原粹树脂。
4. 取消“仅生成计划”，只运行一次脚本。

预期日志顺序：

```text
[周本] 准备刷取“深入风龙废墟”
切换周本队伍
前往七天神像恢复
BetterGI 自动秘境开始
```

确认点：进入的是风魔龙征讨领域；没有使用浓缩、须臾或脆弱树脂；执行结束后有周本奖励或明确的“奖励识别为空/背包复核未确认”提示。

## 测试 2：世界 Boss 执行

1. 将目标文本改为：

```text
雷泽:20>40
```

2. 保持圣遗物填充和路线执行关闭。
3. 填写 Boss 队伍和可选 Boss 战斗策略；队伍必须能击败无相之雷。
4. 先用计划模式确认候选类型是 `boss`、Boss 名称是“无相之雷”，再取消“仅生成计划”运行一次。

预期：日志出现 `[Boss] 准备刷取“无相之雷”`，之后切换 Boss 队伍、前往神像，并由 BetterGI 自动首领执行。它应只消耗原粹树脂，不能使用须臾或脆弱树脂。

确认点：传送路线正确；战斗策略生效；领奖后能继续或在原粹树脂不足时安全结束；若奖励名称为空，执行后背包差值仍应尝试统计“雷光棱镜”。

## 测试 3：圣遗物秘境填充

1. 目标文本改为无材料需求的内容：

```text
香菱:90>90,10/10/10>10/10/10
```

2. 开启“圣遗物秘境填充”，选择“芬德尼尔之顶”，填写圣遗物秘境队伍；保持路线执行关闭。
3. 先用计划模式确认候选任务类型是 `artifactDomain`，名称为“芬德尼尔之顶（圣遗物填充）”。
4. 确认无误后取消“仅生成计划”，只运行一次。

确认点：脚本进入“芬德尼尔之顶”，使用圣遗物队伍和秘境树脂规则；默认不会自动分解圣遗物；邮件/摘要将只记录任务执行状态，不会把圣遗物误记为培养材料收益。

## 失败时需要保留的信息

- BetterGI 完整日志（从 `[初始化]` 到 `[完成]`）；
- `record/latest-plan.json`；
- 当前测试编号与目标文本；
- 使用的队伍/策略名称（可隐藏敏感内容）；
- 实际进入的地图、是否消耗树脂、是否领奖、是否死亡/卡住；
- 若候选映射错误，提供日志中的 `[候选任务]` 和 `[调度]` 两行即可。

不要在失败后连续重试。先恢复“仅生成计划”，保留现场信息后再处理。
