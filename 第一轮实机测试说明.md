# 第一轮实机测试说明

> 归档说明：本文记录 0.9.0 发布前的历史回归步骤，其中“仅生成计划”和周本执行配置已被移除。当前使用方式以 README 和设置页说明为准。

本轮只验证两个执行器：世界 Boss、圣遗物秘境。周本因机制与队伍要求差异大，暂不进行实机测试。

## 开始前共同设置

1. 确认 BetterGI 已处于游戏内可操作状态，队伍名称与配置中填写的文字完全一致。
2. 首次先勾选“仅生成计划”，运行并查看日志；确认候选任务正确后，再取消勾选进行实际执行。
3. 本轮关闭“执行已匹配路线”，世界 Boss 模式选“关闭 → 关闭 Boss”，圣遗物模式选“关闭 → 关闭圣遗物填充”（仅在对应测试时开启）。
4. 树脂策略选“常规树脂 → 浓缩→原粹”；不要在树脂不足时测试。
5. 每次实际执行结束后，保存 BetterGI 日志和 `record/latest-plan.json`；不要立即重复运行，以免消耗额外树脂。

## 测试 0：计划模式安全检查（不消耗树脂）

保持“仅生成计划”开启，并填写 Boss 队伍和圣遗物队伍名称。

目标文本可先使用：

```text
雷泽:20>60
```

预期：日志出现 Boss 候选，类型为 `boss`，目标为“无相之雷”；同时提示 Boss 自动执行默认关闭；不切队、不传送、不消耗树脂。该目标的雷光棱镜需求高于当前已读取的 9 个库存，能够避免“库存已满足而无 Boss 候选”。

若候选类型、材料或目标不正确，停止后保存完整日志，不进行下一步。

## 测试 1：世界 Boss 执行

1. 保持目标文本为 `雷泽:20>60`，并保持圣遗物填充和路线执行关闭。
2. 填写 Boss 队伍；队伍必须能击败无相之雷。确需专用策略时，在高级输入填写 `Boss=策略名称`。
3. 世界 Boss 模式选择“安全测试 → 单次测试 Boss”，只领取 1 次奖励；确认稳定后可改为“正式使用 → 连续刷取 Boss”。
4. 已完成计划模式确认后，取消“仅生成计划”运行一次。

预期：日志出现 `[Boss] 准备刷取“无相之雷”`，之后切换 Boss 队伍、前往神像，并由 BetterGI 自动首领执行。它应只消耗原粹树脂，不能使用须臾或脆弱树脂。

确认点：传送路线正确；战斗策略生效；领奖后能继续或在原粹树脂不足时安全结束；若奖励名称为空，执行后背包差值仍应尝试统计“雷光棱镜”。

## 测试 2：圣遗物秘境填充

1. 目标文本改为无材料需求的内容：

```text
香菱:90>90,10/10/10>10/10/10
```

2. 圣遗物模式选择“单次测试 → 单次｜芬德尼尔之顶”，填写圣遗物秘境队伍；保持路线执行关闭。确需专用策略时，在高级输入填写 `圣遗物=策略名称`。
3. 先用计划模式确认候选任务类型是 `artifactDomain`，名称为“芬德尼尔之顶（圣遗物填充）”。
4. 确认无误后取消“仅生成计划”，只运行一次。

确认点：脚本进入“芬德尼尔之顶”，使用圣遗物队伍和秘境树脂规则；默认不会自动分解圣遗物；邮件/摘要将只记录任务执行状态，不会把圣遗物误记为培养材料收益。

## 失败时需要保留的信息

- BetterGI 完整日志（从 `[初始化]` 到 `[完成]`）；
- `record/latest-plan.json`；
- 当前测试编号与目标文本；
- 使用的队伍/策略名称（可隐藏敏感内容）；
- 实际进入的地图、是否消耗树脂、是否领奖、是否死亡/卡住；
- 若候选映射错误，提供日志中的 `[候选任务]` 和 `[调度]` 两行即可。

不要在失败后连续重试。先恢复“仅生成计划”，保留现场信息后再处理。

## 暂缓：周本

周本仍会计算并在计划中提示，但“周本运行模式”默认选择“关闭 → 关闭周本”。只有为具体周本准备好能稳定通关的专属队伍与战斗策略后，再改为“正式使用 → 启用周本”单独安排测试。
