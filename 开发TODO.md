# 角色一键养成 TODO

最后更新：2026-09-18

状态说明：`[x]` 已完成并验证；`[-]` 已实现、待更多实机验证；`[ ]` 未开始；`[~]` 明确暂缓。

开发节奏：本地测试和接口检查通过后继续实现；游戏内结果暂按接口预期推进，统一留待后续集中回归。所有未实测项保持 `[-]`，不伪装为已验证。

多树脂连续调度、统一失败状态、自动档案和树脂规则自定义的分阶段方案见 [`下一阶段开发计划.md`](下一阶段开发计划.md)。

## 当前可用边界

- 输入角色、武器及培养区间，读取背包并计算材料缺口；不执行角色、武器升级。
- 可自动执行已映射世界 Boss、已映射培养材料秘境与可选圣遗物秘境填充；周本材料只计算缺口并提示手动获取。
- 不刷摩拉、经验书、元素突破玉；不合成浓缩树脂。
- 已映射世界 Boss 和已匹配路线可执行；地方特产/怪物路线仍要求用户已订阅对应路径。
- 当前支持基线为 BetterGI 0.64.0＋OCR V6；更早版本和其他 OCR 实现不做兼容。已对照官方 0.64.1-alpha.1 源码确认秘境、Boss、背包、路线和自动档案接口仍存在；自动秘境显式写入脚本配置的树脂优先级。当前全量 137 项测试通过。

## A. 计算、背包与输入

- [x] 展开角色等级、角色三项天赋、武器等级的材料需求，并合并多目标需求。
- [-] 当前等级位于突破边界时默认计入当前突破档；2026-07-20 已修正 `提纳里:80>90` 漏算月莲/翠翎恐蕈与 `落霞:60>90` 漏算 60 级突破档的问题，待实机核对。
- [x] 排除摩拉、经验书、元素突破玉的自动刷取。
- [x] 读取目标材料及低阶合成材料库存；地方特产读“材料”页，其他培养材料读“养成道具”页。
- [x] 按 3:1 合成链计算天赋书、武器突破材料、怪物材料的折算缺口、合成建议和实际展示缺口。
  - 展示缺口按高→中→低阶分开，不会把全部缺口错误显示为低阶材料。
  - 只按确定的基础配方折算；不预估行秋、优菈等角色天赋的概率返还或额外产出，实际额外收益由下次背包读取计入。已补充“2 个不足、3 个刚好合成”的回归测试。
- [x] 设置页简洁目标输入：`申鹤:70>90,6/8/8>9/9/9；和璞鸢:70>90`。
  - 兼容中英文标点、全角数字、全角箭头和换行。
  - 拦截未知名称、角色/武器混淆、重复项、等级倒退、范围越界和错误天赋格式。
  - JSON 目标文件保留为高级批量输入兜底。
- [x] 设置页输入、背包 OCR 和合成链展示已用不同角色/武器完成实机核对。
- [x] 设置页已按使用频率重排，并在培养目标、培养秘境、地方特产、怪物材料、Boss、圣遗物和高级设置前加入文字分隔；“发送运行摘要”位于全页最底部。角色按元素选择，目标等级和三项天赋使用下拉；角色当前状态由自动档案读取，武器仍支持手动指定。
  - 2026-09-18 已更新至官方 `genshin-db 5.2.13`，下拉列表覆盖当前规则库的 118 名角色和 247 把武器；新增阿罗夏、奥黛塔及 12 把武器。`tools/update-target-selectors.mjs` 会生成选项，规则库生成器完成后自动同步。
  - 2026-09-24 更新至官方 `genshin-db 5.2.14`（原神 7.1），规则库和下拉列表现覆盖 120 名角色、253 把武器；沃雅妮莎、薇斯纳已使用正式培养数据。上游同时把星锋剑的突破消耗改为零；本项目保留该版本原始数据，后续如有实机差异再核对。
  - 2026-08-02 普攻、元素战技和元素爆发已拆成三个独立级联菜单，每项完整覆盖 `1>2` 到 `9>10` 的合法递增组合；未培养项按 `1>1` 参与内部组合，不产生材料需求。
  - 2026-08-02 曾增加自定义培养目标；该公开入口已于 2026-08-29 随自动档案流程收口而移除，旧配置不会继续作为手动目标执行。
  - 2026-08-02 两次仅计划实机验证通过：自定义模式只解析“赛索斯＋幽夜华尔兹”；下拉模式只解析“安柏＋原木刀”及三项 `1>10` 天赋，已保存的自定义文本未混入。两次均完成背包读取、缺口/候选计算、历史保存和通知发送，且没有执行刷取任务。
  - BetterGI 0.64 仍不支持按父开关动态隐藏子项；当前用级联弹窗收纳互斥选项，真正的行级条件显示仍依赖 BetterGI 后续接口支持。
- [x] 默认按 BetterGI 服务器时区和游戏 04:00 刷新规则判断开放日；手动星期仅作高级兜底。

## B. 来源、映射与路线发现

- [x] 从 genshin-db 生成材料 → 候选秘境/开放日、世界 Boss 候选、周本候选和路线候选数据。
- [x] 确认 AutoPathing 目录规律：地方特产通常为 `地方特产/国家/材料名/**/*.json`；怪物通常为 `敌人与魔物/怪物名/**/*.json`，允许作者二级目录。
- [-] 自动发现已订阅的地方特产、怪物材料路线；支持 `data/route-overrides.json` 手动覆盖。2026-07-20 已修正“怪物/作者/路线.json”二级目录漏发现问题，并为来源候选表缺失的新版地方特产增加按材料名发现的兜底，待实机验证。
- [-] 路线执行器：地方特产与怪物材料使用两个独立开关，均默认关闭；开启后调用 `pathingScript.RunFileFromUser()` 并分别切换采集队/怪物材料队。旧版总开关继续兼容。整次运行只在开始前和全部任务结束后读取背包，以总差值统计收益；不再在每条 JSON 后打开背包。
  - 2026-07-20 原始日志确认旧版错误调用 `runFile()`，路径被解析到当前 JS 目录且异常被 BetterGI 内部吞掉；已改用 `User/AutoPathing` 专用接口，无库存增长时标记“未确认”而非成功，待实机验证。
  - 2026-07-20 对照用户现有“怪物材料”“采集”配置组：原生 `Pathing` 项目会自动挂载 `AutoPick`，JS 路线入口不会；现已在路线开始前显式启用同一 BetterGI 自动拾取触发器，并继续继承自动培养配置组的地图追踪/自动战斗参数，待实机验证。
  - 2026-07-20 已修正同一怪物路线因多个材料等级重复执行的问题；同一路线现在只执行一次，并把完整 3:1 材料链纳入最终统一复核。
  - 2026-08-02 已移除每条 JSON 后的背包读取和途中停止判断；本次选中的 JSON 会全部执行，最后统一计算总收益。该取舍避免频繁打断路线，但材料可能少量多刷；本地测试通过，待实机验证整次运行确实只有前后两轮背包复核。
  - [x] 2026-08-01 月莲 10 条路线实机获得 `49` 个；蕈兽共享路线只执行一次，并确认蕈兽孢子 `+4`、荧光孢粉 `+2`、孢囊晶尘 `+1`。
  - [x] 2026-08-01 错误采集队实机复测通过：只执行“首次切换＋神像重试”两次，失败后不再对 10 条路径循环切队，并继续切换怪物队执行蕈兽路线。
  - 已匹配路线材料会在本次计划中标记为自动路线来源并移出人工待办；路线未接入历史完成时间预估前仍不显示预计天数。
  - 待集中实机回归路线兼容性、切队、卡死/战斗失败和 OCR 差值。
- [x] 已验证并启用两条 BetterGI 秘境名称映射：
  - `精通秘境：炽炎祭场` → 太山府 → 「繁荣」；
  - `炼武秘境：雷云祭坛` → 震雷连山密宫 → 孤云寒林。
- [x] 周日奖励序号通用规则：周一/四组=第 1 项、周二/五组=第 2 项、周三/六组=第 3 项；映射可为例外显式覆盖。2026-08-01 计划模式确认昏识塔「巧思」传入 `2`、「笃行」传入 `3`；2026-08-02 昏识塔第 3 项已完成 3 次浓缩树脂实际领奖，第 2 项仍待实测。
- [-] 已补全 48 条“培养材料来源关卡 → BetterGI `DomainName`”映射，覆盖 16 个材料秘境与 165 个材料条目。
  - 数据由 genshin-db 来源关卡与 BetterGI 内置 `tp.json` 奖励列表交叉对照生成；首批太山府「繁荣」已实机，其余待集中回归。
  - 2026-09-18 新增至冬培养秘境“荒坠的圣迹”和武器秘境“妄念的创痕”，并把“逆悬的冰河”加入圣遗物填充列表；名称已由 BetterGI 0.64.0 `tp.json` 核对，尚未实机回归。
  - 阿罗夏与奥黛塔的材料计算已生成；其新世界 Boss 在 BetterGI 0.64.0 中尚不受自动首领支持，因此当前只列为手动获取，不会错误调用。BetterGI 0.65.0 已加入对应 Boss，但在完成版本升级与实机验证前不提前启用。
  - 7.1 两名新角色的「坚忍」和「荣光」系列已映射至荒坠的圣迹（周二/五/日、周三/六/日）；本机已订阅霜仙花、金蕨、异种合成魔兽及肌生晶石的妖精路线。BetterGI 0.65.0 支持沃雅妮莎所需的“不灭衍生造物”，0.64.x 不支持；“游雪的护刃”在 0.65.0 仍未受支持。稳定版最低兼容 0.64.0，两项突破 Boss 暂保持手动，待版本能力适配与实机验证。
  - 回归失败时在 `codex/test-domain-mapping` 分支修正；不得用未经来源/奖励表对照的名称新增映射。
- [x] 规则库生成器使用 `data/bettergi-domain-catalog.json` 校验所有 `DomainName`，未知名称会在生成阶段失败。
- [ ] 为每条映射保存验证版本、验证日期、测试材料和失败原因。
- [ ] 路线覆盖支持目录展开，并校验文件可读、路线类型和特殊队伍需求。

## C. 秘境执行、树脂与安全

- [x] 构造 `AutoDomainParam` 并调用 `dispatcher.RunAutoDomainTask()`；支持队伍、战斗策略和指定树脂次数。
- [x] 秘境前切换指定队伍、传送七天神像恢复；切队失败会停止任务。
  - 统一顺序为“先传送七天神像，后切换队伍”，规避特殊地点禁止切队。
- [x] 不合成树脂；优先消耗已有浓缩树脂，再消耗原粹树脂。
  - 须臾、脆弱树脂默认关闭，须由用户显式开启。
- [x] 培养秘境单次测试：从级联运行模式选择后，强制只领取 1 次原粹树脂奖励，并禁用浓缩、须臾、脆弱树脂；2026-08-01 已分别在无光的深都和昏识塔完成一次原粹树脂领奖并确认材料收益。
- [x] 周日没有有效奖励序号时拒绝执行，防止领取错误材料。
- [-] 太山府已实机执行，确认不会再前往合成台，且会正确传入周日第 1 项。
- [x] 秘境、Boss 和圣遗物任务默认开启任务级奖励识别；整次运行只在开始前与全部任务结束后读取背包。背包结果可信时优先，结束背包漏识别时使用任务奖励兜底，且不重复相加。
  - 任务奖励字典参与背包漏识别时的收益兜底，并在历史中保存采用来源与冲突证据；邮件继续独立展示路线成功、失败或未确认状态。
  - 2026-08-28 修复启动背包未找到材料、结束背包又识别成功时，把结束库存总量误算成本次收益的问题。开始/结束两次扫描分别保存图标未匹配证据；任一端不可靠时不采用原始背包差值，有任务奖励则以任务奖励兜底，无任务奖励则保持收益未确认。路线收益使用同一可信度规则。新增 `猎兵祭刀 0→2951` 日志回归用例，避免再次虚报大量收益。
  - 2026-07-20 实机发现执行后复查会偶发把已有高阶材料从 21 识别为 0；已增加同次运行库存不下降保护，待回归验证。
  - 2026-08-08 对照 BetterGI 接口和连续实机日志后，正式路径不再读取返回值；无背包增长证据时只写“任务调用结束，未确认是否成功领奖”，不再写成“已执行完成”。
- [-] 整次运行开始前读取一次目标材料和合成链，树脂任务与路线全部结束后再统一读取一次并记录总差值；中途不打开背包。待验证 OCR 对不同材料的稳定性。
- [x] 公开版新增顶层“我已确认配置并允许实际执行”复选框，默认不勾选；未确认时在读取背包、写文件、发送通知或操作游戏之前立即拒绝运行。原“仅生成计划”模式已移除。
- [x] 执行确认已移动到设置页使用说明下方；设置页和 README 明确要求优先使用 OCR V6，背包 OCR 失败日志会给出同一排查建议。脚本不宣称能够检测或切换 OCR 版本。
- [-] 自定义树脂模式可按开始时缺口连续执行最多四类树脂任务；每类只选择排序最前的一个具体目标，每项最多调用一次，全部任务结束后统一复核。兼容模式仍只执行首个任务。100 项本地测试通过，待低额度实机验证连续调用和树脂不足退出。
- [ ] 遇到秘境未开放、识别失败、战斗失败、传送/切队失败时，按材料记录失败原因并安全停止或跳过。

## D. 调度、记录与通知

- [-] 统一执行结果与失败状态模型：新增 `completed/unconfirmed/skipped/failed` 四种结果、稳定 `code/stage/severity/retryable` 字段以及任务/警告列表；配置、背包、切队、秘境、Boss、路线、奖励复核和安全退场已接入。旧 `execution.task/status/reason` 与旧历史记录继续兼容；邮件和历史可显示未确认结果及非覆盖性清理警告。83 项本地测试通过，待实机核对 BetterGI 实际异常文本、树脂不足和邮件排版。
- [x] 同一秘境、同日、多阶材料合并为一次任务，避免重复进入。
- [x] 生成从今天起的七天循环策略，并在日志、计划记录和运行摘要中显示每天可刷的秘境。
- [x] 当前树脂任务排序：世界 Boss → 当日开放培养秘境（同类中限时项优先）→ 圣遗物填充；地方特产/怪物路线不耗树脂，不参与树脂排序。
  - 周本材料固定进入“手动获取”列表，不阻塞 Boss、培养秘境或路线；Boss 默认关闭，须由用户确认机制、专属队伍与战斗策略后显式开启。
  - 兼容模式一次只选择首个已启用树脂任务；自定义模式按配置连续执行最多四类任务、每类一个具体目标。任务失败、奖励未确认或明确树脂不足时停止后续树脂任务；不在同次运行内精确分配多个同类秘境的树脂。
- [x] 配置页与正式运行日志已写明当前实际规则：按固定任务顺序选择首个合格任务；正式模式对该任务依次尽量用完所选树脂，不按材料缺口中途停止；Boss 只用原粹，培养/圣遗物秘境按所选树脂类型的 BetterGI 固定顺序消耗。
- [x] 保存 `record/latest-plan.json` 和最多 100 条的 `record/history.json`。
- [x] 完成日志按实际内容区分“树脂任务”“路线任务”和“未执行”，不再统一显示“至多一个已验证任务”。
- [-] 邮件/通知摘要：已调用 `notification.Send()`，已启用 `js.custom`；正文采用 HTML 分段显示本次状态、收益、缺口、候选和预计完成。
  - [x] 2026-08-04 至 08-07 连续邮件确认 HTML 分段与附件正常。
  - [x] 公开版 README 使用 `2739252596@qq.com` 作为问题与需求反馈邮箱；Bug 反馈提示附 BetterGI 版本、脚本版本、复现步骤和完整日志，并提醒发送前检查日志隐私。
  - [x] 2026-08-09 正式路径已关闭任务级奖励识别：摘要把“本次任务”和“背包确认收益”分开；圣遗物填充明确不纳入培养材料计数；背包无增长时不再声称执行成功。同日“月童的库藏”正式运行邮件已核对新文案。
  - [x] 2026-08-14 按实际使用反馈重新默认开启培养秘境、世界 Boss 和圣遗物秘境的 BetterGI 任务级奖励识别；每轮识别结果用于日志排查，邮件与历史收益仍以整次运行前后的背包差值为准。
  - [x] 2026-08-14 修复结束背包复核把“未找到物品”误判为库存 0：首次扫描仍允许未找到=0，任务结束时改为保留执行前数量并记录“背包识别失败/奖励结果未知”；邮件和历史记录不再把扫描漏识别写成零收益。
  - [x] 2026-08-15 根据守望者·堕天实机日志接入任务奖励字典兜底：四次 `堕天的落羽×3` 可合并为 `×12`，结束背包漏识别时用于更新缺口；背包读取改用 ItemV2，并将图标未匹配与数量 OCR 失败分开提示。代码与模拟回归已完成，待下一次正常树脂运行复核。
  - [x] 2026-08-15 补齐 ClearScript `.NET Dictionary` 的 `get_Item()` 与枚举器读取方式，避免 BetterGI 已识别奖励但脚本转换为空；新增模拟回归，仍待下一次实际领奖复核。
  - [x] 2026-08-28 邮件将“下一步候选”改为更准确的“今日可执行任务”；当天开放但本次树脂可能已耗尽时，不再写“最早今天”，改为提示等待下一个开放日。相关收益证据、路线和预计完成回归后，全量 119 项测试通过。
  - [x] 2026-09-01 修复跨次运行的背包漏识别：若历史最近一次确认材料为正数、本次 ItemV2 却未找到，不再直接按零重复调度，而是把对应 3:1 合成链标记为库存未确认并暂停相关任务。材料已满足时，邮件改为“无需执行/培养材料已满足”，不再显示开放日文案。
  - [x] 2026-09-22 根据连续运行日志调整跨次冲突策略：历史有库存但本次未找到时只提示重复获取风险，按零继续计算和刷取，不再让同一培养目标连续多天空跑。
  - [x] 2026-09-15 根据连续运行邮件修正库存不确定状态：摘要会列出“材料名（库存未确认）”，不再同时显示“仍缺材料：无”。
- [-] 根据已确认背包差值建立历史均值，估算自动秘境材料的还需运行批次/天数；样本不足、收益为零或含未自动来源时明确显示原因。
  - [ ] 路线材料尚未接入历史样本预估；需按路线执行记录中的背包实际增量计算每轮收益。接入前统一不显示预计天数，避免给出虚假工期。
  - 待用真实背包差值样本回归估算是否符合实际。
- [-] 单次运行按有界队列分配任务：完成后继续，失败、奖励未确认或明确树脂不足时停止；没有无限循环。因 BetterGI 不公开可靠剩余树脂状态，当前不尝试动态重排或反复探测。

## E. 待实机回归的树脂任务

- [-] 世界 Boss 执行器：已映射 39 项材料到 BetterGI 内置首领，复用 `RunAutoBossTask()`；只使用原粹树脂、支持专属 Boss 队伍/策略，失败会停止本次后续任务。
  - [x] 2026-07-20 急冻树单次实机通过：自动前往、自动战斗、领取一次奖励、奖励识别和背包前后差值均成功；极寒之核 `8 → 11`，确认收益 `×3`。空策略会复用 BetterGI 全局“自动首领”的战斗策略。
  - [x] 隐山猊兽已在 2026-08-01 完成自动战斗和领奖，凝云鳞甲确认 `+3`；霜夜巡天灵主已在 2026-08-05 完成自动战斗和领奖，漫光的辉角确认 `+3`。
  - [-] 守望者·堕天和蕴光月幻蝶已有实际任务调用记录，但最终背包未确认目标材料增长；其余 Boss（含机制特殊的首领）仍待逐项回归，不得默认视为稳定通过。
  - [x] 2026-08-29 将 12 项 Boss 专属配置槽压缩为“开关＋文本框”；随后改为更易读的 `Boss:队伍=名称,策略=名称` 和 `Boss:禁用`，名称、重复项和格式均在操作游戏前校验，并兼容旧文本及结构化配置。
  - [x] 2026-08-15 修复原粹树脂不足结束时仍停留在已刷新的 Boss 面前：保持“不每轮返回神像”，仅在整个自动首领调用结束或异常后尝试传送一次七天神像。退场失败只记录警告，不覆盖原始任务结果或异常；待下一次 Boss 运行验证后续一条龙可正常衔接。
  - [x] 设置页与 README 明确提示：一条龙中不要在本脚本后重复启用 BetterGI 内置“自动秘境”或“自动首领讨伐”，避免重复消耗树脂及混淆任务错误来源。
- [~] 周本自动执行不进入 0.9.0 公开版：37 项材料映射保留用于缺口计算与手动提示，执行器和设置项已移除。原因是机制、队伍、周次数限制及 30/60 树脂界面差异显著，尚不足以作为安全默认能力公开。
- [x] 圣遗物秘境：纳入 0.9.0，已实现为没有已启用 Boss 或培养材料秘境时的可选填充任务；通过一个级联菜单同时选择关闭/单次/正式填充和目标秘境，支持独立队伍/策略，默认不自动分解圣遗物。2026-08-06、08-07、08-09 的“月童的库藏”日志已验证完整执行链路；其他秘境尚未逐个回归。

## F. 完成预估

- [-] 预计完成时间采用用户确认的世界等级 9 固定机制：世界 Boss 按 `3.1` 个/40 树脂；培养材料秘境按最高难度掉落期望；每日树脂预算默认 180。
  - 周本与圣遗物不显示预计天数；周本缺口不会遮挡同一目标中可自动执行的 Boss/秘境预计；路线历史背包差值预估尚未接入，当前不显示预计天数；不考虑双倍掉落。
  - [x] 单元测试覆盖最高难度培养秘境、世界 Boss 和周本不预估规则。
  - [x] 2026-07-20 急冻树仅计划模式验证通过：极寒之核缺口 `15` 正确预估为 `5` 次领奖、约 `200` 树脂、约 `1` 天；日志、邮件与 `latest-plan.json` 一致，未消耗树脂。
  - [-] 培养秘境显示“预计领奖次数、树脂、所需开放日、从当前日期起的最早自然日”；世界 Boss 改为显示“预计领奖次数、树脂、按每日树脂预算约需天数”，不再误称开放日。待下一封真实邮件检查长度与排版。

## G. 自动读取当前档案并生成目标（代码已完成，待实机验证）

- [x] 确认目标输入边界：用户仍须选择角色目标等级、普通攻击/元素战技/元素爆发各自的目标等级和自动适配武器的目标等级；只自动读取当前值与佩戴武器，不自动决定培养上限。
- [x] 自动档案已进入当前 0.9.0。以 BetterGI 0.64.0 的 `characterDevelopmentTask.GetCharacter()` 为基线；实现会先检测接口能力，缺少接口时停止自动模式并提示升级。
- [x] 已记录此前 OCR V6 测试基线：25 名角色首轮、11 名重点角色重复读取均成功并覆盖五种武器筛选类型；当时返回的武器和天赋字段为空，因此只证明角色定位、筛选与等级读取链路。
- [-] 三项天赋会读取界面等级和命座加成标记；`+3` 在生成目标前还原为实际培养等级。模拟测试已覆盖，待零命与命座角色实机核对。
- [-] 当前佩戴武器名称、等级和等级上限已接入；武器名必须存在于规则库，未知新武器会停止，不猜测。待五种武器类型完整档案实机核对。
- [x] 规则库增加武器稀有度；`rarity=1` 自动忽略无锋剑、训练大剑、新手长枪、学徒笔记和猎弓。
- [x] 新增 `core/character-profile.js` 档案适配器和来源字段；第一版只使用 BetterGI 游戏内接口，不依赖浏览器登录或米游社数据。
- [x] 识别结果已转换为现有目标格式，并接入 `latest-profile.json`、计划、历史、日志和邮件。角色与武器使用 `{ level, levelLimit }` 区分突破前后；识别失败会在背包扫描和调度前停止。
- [x] 设置页取消角色手动填写，只提供“预览目标”和“读取档案并执行”，以及角色/三项天赋/武器的目标下拉。武器支持自动读取、不培养和手动指定；旧手动模式保存值迁移为正式档案读取。
- [x] 新增 14 项专项测试，覆盖能力缺失、字段校验、`80/80` 与 `80/90`、命座 `+3`、一星武器、三种武器模式、等价手动需求、邮件、历史和预览提前结束；全量 114 项通过。
- [ ] 实机覆盖：零命与命座加天赋角色、已满级角色、普通武器、一星初始武器、满级武器、旅行者、新角色未收录和 OCR 失败。

## H. 树脂优先级与规则自定义（开发中）

- [x] 将“任务类型优先级”与“秘境允许使用的树脂类型”建模为两个独立配置；新增不可变 `resinPolicyV2`，旧 `resinStrategy` 自动迁移且默认行为不变。
- [x] 增加“一次只刷一类（推荐）”和“按顺序刷多类”两种模式，并兼容旧名称。多类模式可排列世界 Boss、限时培养秘境、普通培养秘境、圣遗物填充，并把地方特产/怪物路线设在树脂任务前或后；周本不进入排序。
- [x] 2026-08-29 精简设置页长说明；取消角色手动填写，保留手动指定武器。“全部”改名为“用完可用树脂”，旧值继续兼容。确认执行后若初始化异常，可发送失败摘要。
- [x] 同步简化战斗策略和 Boss 专属配置文案：策略留空继承 BetterGI 对应全局配置，Boss 推荐格式改为带字段名的 `Boss:队伍=名称,策略=名称`。本轮 122 项测试、脚本语法、JSON 和树脂规则校验通过，待实机查看设置页排版及自动档案完整字段。
- [x] 2026-08-30 将四组任务顺序和四组领奖上限合并为四个“任务与次数”级联槽；旧分离字段继续兼容。多个 Boss 专属项明确使用分号分隔，`Boss:禁用` 仅用于阻止自动匹配到的目标 Boss 继承通用配置执行。未标注突破状态的等级统一按突破前计算；本轮 123 项测试通过。
- [x] 2026-08-30 将培养内容收敛为“角色和当前武器、角色和指定武器、仅角色、仅指定武器”四种模式，并兼容旧三种武器处理值。角色等级、三项天赋和武器按项目独立判断：达到或超过目标时跳过，单项目识别失败不再连带终止其他有效目标；仅指定武器不读取角色档案，全部目标完成时不读取背包或执行任务。目标状态写入最新档案、历史和邮件摘要；全量 127 项测试通过。
- [-] 四类任务的本次最大领奖次数已经接入连续任务队列：Boss 使用 `RunCount`；有限秘境预算只交给第一种已授权树脂，避免 BetterGI 按树脂类型分别计数导致超限。待实机确认不同树脂组合的退出表现。
- [x] 须臾和脆弱树脂保持独立授权、默认关闭；Boss 仍固定只使用原粹树脂，培养和圣遗物秘境仍受 BetterGI 固定内部顺序限制。
- [~] 原粹树脂精确保留量暂不实现：BetterGI 当前没有向 JS 脚本公开可靠的剩余树脂查询接口，不能用领取次数伪装成精确保留量。
- [ ] 研究按运行开始时的材料缺口限制最大领取次数；保持“整次运行只读前后两次背包”，不为追求刚好补齐而在每次领奖后打开背包。内部奖励识别不稳定时必须明确这是预算上限而非精确完成判断。
- [x] 自定义规则不能绕过开放日、真实材料缺口、执行确认、队伍必填、已验证映射、周日奖励序号和 Boss 树脂限制；执行器原有硬约束保持独立校验。
- [x] 配置使用中文下拉项；重复任务、未知任务、空槽、零/负数/小数和超大次数都会在读取背包或操作游戏前拒绝运行。
- [x] 新增 `pnpm validate:resin` 独立校验命令，默认读取设置页默认值，也可传入一份设置 JSON，输出规范化任务顺序、领取上限、允许树脂和路线时机。
- [x] 自定义优先级只控制顺序和上限，不实现多个秘境之间的精确比例分配。

## I. 缺失路线自动匹配与原生订阅（预研）

- [x] 核对当前接口边界：`pathingScript` 可以检查、读取和运行路线，但没有向普通 JS 脚本公开仓库订阅或写入 `User/AutoPathing` 的接口；BetterGI 内部原生导入会要求用户确认。
- [x] 确定安全边界：不在脚本内通过 HTTP 静默下载路线，不绕过 BetterGI 原生确认，不把整个材料目录当作默认订阅目标；该功能不进入 0.9.0 和官方 PR #3496。
- [ ] 设计并生成版本化 `route-catalog.json`：记录材料名称/别名、地方特产或怪物类型、官方仓库精确相对路径、作者/路线变体、特殊角色或队伍要求、验证状态和目录版本。
- [ ] 建立目录生成与校验工具：与官方仓库现有路线树交叉检查，拒绝不存在路径、绝对路径、`..` 目录穿越、重复项和类型不一致；仓库目录变化时输出明确差异。
- [ ] 制定路线选择规则：默认只推荐已核对的具体路线包；同一材料存在全收集、高效、草神或多作者版本时要求用户选择，不自动订阅整个材料目录。
- [ ] 在计算缺口后生成三类状态：已安装可执行、官方仓库可订阅、暂无可靠路线；只有实际仍缺材料才进入订阅建议。
- [ ] 生成 `record/route-subscription.txt` 与 BetterGI 原生导入数据/链接，列出材料、路线来源、变体和要求；用户确认导入并重新运行后，由现有路线发现器匹配执行。
- [ ] 保留并完善 `data/route-overrides.json`，作为官方仓库无路线、用户自建路线或目录暂时失效时的人工兜底。
- [ ] 向 BetterGI 提议受控的 JS 订阅接口：仅允许官方仓库 `pathing/` 相对路径，强制原生确认，由主程序负责订阅记录、更新、去重和回滚，并返回安装/取消/缺失/失败状态。
- [ ] 若官方接口发布，增加版本与能力检测：支持时在用户确认后重新扫描并继续本次运行；不支持时自动退回订阅助手，不把缺少接口视为脚本错误。
- [ ] 自动测试目录索引、别名映射、URI 编码、已安装去重、多候选、失效路径、用户取消、非活动仓库和不安全路径拦截。
- [ ] 后续实机覆盖一个地方特产和一个怪物材料：首次订阅、重复订阅、取消确认、路线变体选择、订阅后重新发现与执行。
- [~] 第三方路线仓库自动接入暂缓；需先确定来源标识、作者许可、内容校验、更新与回滚规则，不能与官方仓库路线混为同一可信等级。

验收：只选择角色培养目标即可获得准确的缺失路线和官方订阅建议；用户完成 BetterGI 原生确认后能够被脚本重新发现，不会静默下载、重复执行或误订阅整目录。

## J. 开发环境稳定性

- [x] 统一使用项目已可用的 pnpm，`package.json` 锁定 pnpm 11.9.0 与 Node.js 22+；不再调用会跳转到沙箱不可读用户全局目录的 npm。
- [x] `genshin-db` 固定从项目本地 `node_modules` 解析；项目级 `AGENTS.md` 禁止假定 Codex 公共运行时已经安装该依赖。
- [x] 增加项目级 `.gitattributes`，统一文本文件为 LF，并显式标记常见二进制文件。
- [x] 仓库本地 Git 配置已改为使用项目 `.gitignore`，`git status` 不再访问沙箱不可读的默认全局 ignore 路径，也不再产生权限警告。
- [x] 项目代理约定普通自动化命令不加载用户 PowerShell profile，避免非交互输出触发无关的 PSReadLine 预测提示警告。

## 下一步顺序

1. [x] 回应官方 PR 首轮审查：README 已补充与现有养成脚本的区别和目标驱动优势，明确合成天赋的保守计算边界；官方发布子集不再重复包含许可证文件。
2. 完成 0.9.0 修订版发布校验，同步个人源码仓库与官方 PR。
3. 下次 Boss 正常运行时复核任务奖励字典、结束后传送神像及后续一条龙衔接；继续记录未覆盖 Boss 的机制与专属队伍需求。
4. 使用自愿单次模式回归更多培养秘境映射，并继续记录验证版本；周日第 3 项已实测，后续只需补第 2 项和其他未覆盖秘境。
5. 回归“运行前一次＋全部任务结束后一次”的统一背包复核：两端可信时优先采用整次运行背包差值；任一端漏识别时使用 BetterGI 任务奖励兜底且不重复累计；两种证据都缺失时保持未确认。
6. [-] 多类树脂任务有界连续调度已完成代码与本地测试；完成判断继续使用整次运行前后背包复核，预计完成模型仍按现有来源规则计算，待集中实机回归。
7. [x] BetterGI 0.64.0 自动档案适配器、设置、记录和邮件已完成，并已纳入日常运行；角色与武器目标彼此独立，单项识别失败不会再连带丢失另一项目标。
8. [x] 完成树脂优先级/规则自定义的 V2 配置模型及有界多树脂队列；不开发多秘境精确比例分配，实机回归暂缓。
9. 开发缺失路线订阅助手的目录索引与原生导入数据原型；先不接入 HTTP 下载，也不等待 BetterGI 新接口。
