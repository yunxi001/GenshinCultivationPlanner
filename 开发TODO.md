# 养成材料调度器 TODO

最后更新：2026-07-20

状态说明：`[x]` 已完成并验证；`[-]` 已实现、待更多实机验证；`[ ]` 未开始；`[~]` 明确暂缓。

开发节奏：本地测试和接口检查通过后继续实现；游戏内结果暂按接口预期推进，统一留待后续集中回归。所有未实测项保持 `[-]`，不伪装为已验证。

## 当前可用边界

- 输入角色、武器及培养区间，读取背包并计算材料缺口；不执行角色、武器升级。
- 自动执行已验证的培养材料秘境：太山府的「繁荣」、震雷连山密宫的「孤云」。
- 不刷摩拉、经验书、元素突破玉；不合成浓缩树脂。
- Boss、周本、地方特产路线、怪物路线当前只展示候选/缺口，不执行。

## A. 计算、背包与输入

- [x] 展开角色等级、角色三项天赋、武器等级的材料需求，并合并多目标需求。
- [x] 排除摩拉、经验书、元素突破玉的自动刷取。
- [x] 读取目标材料及低阶合成材料库存；地方特产读“材料”页，其他培养材料读“养成道具”页。
- [x] 按 3:1 合成链计算天赋书、武器突破材料、怪物材料的折算缺口、合成建议和实际展示缺口。
  - 展示缺口按高→中→低阶分开，不会把全部缺口错误显示为低阶材料。
- [x] 设置页简洁目标输入：`申鹤:70>90,6/8/8>9/9/9；和璞鸢:70>90`。
  - 兼容中英文标点、全角数字、全角箭头和换行。
  - 拦截未知名称、角色/武器混淆、重复项、等级倒退、范围越界和错误天赋格式。
  - JSON 目标文件保留为高级批量输入兜底。
- [-] 设置页输入、背包 OCR 和合成链展示已本地测试；待用不同角色/武器各完成一次实机核对。

## B. 来源、映射与路线发现

- [x] 从 genshin-db 生成材料 → 候选秘境/开放日、世界 Boss 候选、周本候选和路线候选数据。
- [x] 确认 AutoPathing 目录规律：地方特产为 `地方特产/国家/材料名/*.json`；怪物为 `敌人与魔物/怪物名/*.json`。
- [-] 自动发现已订阅的地方特产、怪物材料路线；支持 `data/route-overrides.json` 手动覆盖。
  - 当前只发现、记录和提示，不执行。
- [x] 已验证并启用两条 BetterGI 秘境名称映射：
  - `精通秘境：炽炎祭场` → 太山府 → 「繁荣」；
  - `炼武秘境：雷云祭坛` → 震雷连山密宫 → 孤云寒林。
- [x] 周日奖励序号通用规则：周一/四组=第 1 项、周二/五组=第 2 项、周三/六组=第 3 项；映射可为例外显式覆盖。
- [ ] 补全并逐项核验“所有培养材料秘境来源 → BetterGI `DomainName`”映射。
  - 未验证名称必须保持不可执行；不得依据名称猜测自动刷取。
- [ ] 为每条映射保存验证版本、验证日期、测试材料和失败原因。
- [ ] 路线覆盖支持目录展开，并校验文件可读、路线类型和特殊队伍需求。

## C. 秘境执行、树脂与安全

- [x] 构造 `AutoDomainParam` 并调用 `dispatcher.RunAutoDomainTask()`；支持队伍、战斗策略、奖励识别和指定树脂次数。
- [x] 秘境前切换指定队伍、传送七天神像恢复；切队失败会停止任务。
- [x] 不合成树脂；优先消耗已有浓缩树脂，再消耗原粹树脂。
  - 须臾、脆弱树脂默认关闭，须由用户显式开启。
- [x] 周日没有有效奖励序号时拒绝执行，防止领取错误材料。
- [-] 太山府已实机执行，确认不会再前往合成台，且会正确传入周日第 1 项。
- [-] BetterGI 奖励识别在实机中多次返回空；脚本已明确标记“未确认收益”，不会把空结果当成零掉落。
- [-] 执行后会重扫目标材料和合成链材料，并记录前后差值；待验证 OCR 对不同材料的稳定性。
- [ ] 每轮领取后重新计算库存、缺口和剩余树脂，再选择下一秘境；当前一次运行只执行一个已验证秘境任务。
- [ ] 遇到秘境未开放、识别失败、战斗失败、传送/切队失败时，按材料记录失败原因并安全停止或跳过。

## D. 调度、记录与通知

- [x] 同一秘境、同日、多阶材料合并为一次任务，避免重复进入。
- [x] 调度顺序模型已预留：周本 → 世界 Boss → 限时培养秘境 → 无树脂路线。
  - 由于 Boss/周本暂缓，当前实际执行仅限已验证秘境。
- [x] 保存 `record/latest-plan.json` 和最多 100 条的 `record/history.json`。
- [-] 邮件/通知摘要：已调用 `notification.Send()`，已启用 `js.custom`；正文采用 HTML 分段显示本次状态、收益、缺口、候选和预计完成。
  - 待确认真实邮件客户端的最终排版。
- [ ] 根据背包差值和历史掉落建立统计，估算还需运行次数/天数；没有足够样本时明确显示“暂无法估算”。
- [ ] 保存可恢复的周循环进度，并在下次运行时从未完成缺口继续。
- [ ] 在单次运行中循环分配可用树脂，直至无可执行材料、树脂不足或发生安全错误。

## E. 明确暂缓

- [~] 世界 Boss 执行器、40 树脂规则、专属 Boss 队伍/战斗策略与失败停止。
- [~] 周本执行器、每周次数限制、30/60 树脂规则与周本队伍。
- [~] 地方特产/怪物路线实际执行和背包差值统计。
- [~] 圣遗物秘境：培养材料流程稳定后再决定是否开发。

## 下一步顺序

1. 补全并验证全部培养材料秘境的 BetterGI 名称映射。
2. 用不同秘境实测：周日奖励选择、队伍切换、神像恢复、原粹/浓缩树脂和背包复核。
3. 修复或替代奖励识别为空的问题，以背包差值建立可靠收益统计。
4. 实现多秘境循环调度、完成判断与预计完成天数。
5. 再评估是否恢复 Boss、周本和路线执行开发。
