# 角色一键养成 TODO

最后更新：2026-08-09

状态说明：`[x]` 已完成并验证；`[-]` 已实现、待更多实机验证；`[ ]` 未开始；`[~]` 明确暂缓。

开发节奏：本地测试和接口检查通过后继续实现；游戏内结果暂按接口预期推进，统一留待后续集中回归。所有未实测项保持 `[-]`，不伪装为已验证。

## 当前可用边界

- 输入角色、武器及培养区间，读取背包并计算材料缺口；不执行角色、武器升级。
- 可自动执行已映射世界 Boss、已映射培养材料秘境与可选圣遗物秘境填充；周本材料只计算缺口并提示手动获取。
- 不刷摩拉、经验书、元素突破玉；不合成浓缩树脂。
- 已映射世界 Boss 和已匹配路线可执行；地方特产/怪物路线仍要求用户已订阅对应路径。

## A. 计算、背包与输入

- [x] 展开角色等级、角色三项天赋、武器等级的材料需求，并合并多目标需求。
- [-] 当前等级位于突破边界时默认计入当前突破档；2026-07-20 已修正 `提纳里:80>90` 漏算月莲/翠翎恐蕈与 `落霞:60>90` 漏算 60 级突破档的问题，待实机核对。
- [x] 排除摩拉、经验书、元素突破玉的自动刷取。
- [x] 读取目标材料及低阶合成材料库存；地方特产读“材料”页，其他培养材料读“养成道具”页。
- [x] 按 3:1 合成链计算天赋书、武器突破材料、怪物材料的折算缺口、合成建议和实际展示缺口。
  - 展示缺口按高→中→低阶分开，不会把全部缺口错误显示为低阶材料。
- [x] 设置页简洁目标输入：`申鹤:70>90,6/8/8>9/9/9；和璞鸢:70>90`。
  - 兼容中英文标点、全角数字、全角箭头和换行。
  - 拦截未知名称、角色/武器混淆、重复项、等级倒退、范围越界和错误天赋格式。
  - JSON 目标文件保留为高级批量输入兜底。
- [x] 设置页输入、背包 OCR 和合成链展示已用不同角色/武器完成实机核对。
- [x] 设置页已按使用频率重排，并在培养秘境、地方特产、怪物材料、Boss、圣遗物和高级设置前加入“上下空行＋接近整行宽度的重线文字标题＋原生分隔线”；“发送运行摘要”已移至全页最底部。角色按元素、武器按类型提供级联下拉，角色/武器等级及角色三项天赋也分别使用级联选择；非常规等级、天赋组合和多目标通过显式开关启用自定义输入。旧目标文本兼容、输入模式切换和配置弹窗已通过 BetterGI 实机检查。
  - 2026-08-02 下拉列表已覆盖当前规则库的 118 名角色和 235 把武器；`tools/update-target-selectors.mjs` 会生成选项，规则库生成器完成后自动同步。
  - 2026-08-02 普攻、元素战技和元素爆发已拆成三个独立级联菜单，每项完整覆盖 `1>2` 到 `9>10` 的合法递增组合；未培养项按 `1>1` 参与内部组合，不产生材料需求。
  - 2026-08-02 增加“启用自定义培养目标”复选框：未勾选时忽略自定义文本，勾选后只采用自定义文本并忽略上方全部目标选择；空自定义目标会被拦截。世界 Boss 标题缩短为左右各 18 个重线字符，避免右半段整体换行。56 项本地测试通过。
  - 2026-08-02 两次仅计划实机验证通过：自定义模式只解析“赛索斯＋幽夜华尔兹”；下拉模式只解析“安柏＋原木刀”及三项 `1>10` 天赋，已保存的自定义文本未混入。两次均完成背包读取、缺口/候选计算、历史保存和通知发送，且没有执行刷取任务。
  - BetterGI 0.62.0 不支持按父开关动态隐藏子项；当前用级联弹窗收纳互斥选项，真正的行级条件显示仍依赖 BetterGI 后续接口支持。
- [x] 默认按 BetterGI 服务器时区和游戏 04:00 刷新规则判断开放日；手动星期仅作高级兜底。

## B. 来源、映射与路线发现

- [x] 从 genshin-db 生成材料 → 候选秘境/开放日、世界 Boss 候选、周本候选和路线候选数据。
- [x] 确认 AutoPathing 目录规律：地方特产通常为 `地方特产/国家/材料名/**/*.json`；怪物通常为 `敌人与魔物/怪物名/**/*.json`，允许作者二级目录。
- [-] 自动发现已订阅的地方特产、怪物材料路线；支持 `data/route-overrides.json` 手动覆盖。2026-07-20 已修正“怪物/作者/路线.json”二级目录漏发现问题，并为来源候选表缺失的新版地方特产增加按材料名发现的兜底，待实机验证。
- [-] 路线执行器：地方特产与怪物材料使用两个独立开关，均默认关闭；开启后调用 `pathingScript.RunFileFromUser()` 并分别切换采集队/怪物材料队。旧版总开关继续兼容。整次运行只在开始前和全部任务结束后读取背包，以总差值统计收益；不再在每条 JSON 后打开背包。
  - 2026-07-20 原始日志确认旧版错误调用 `runFile()`，路径被解析到当前 JS 目录且异常被 BetterGI 内部吞掉；已改用 `User/AutoPathing` 专用接口，无库存增长时标记“未确认”而非成功，待实机验证。
  - 2026-07-20 对照用户现有“怪物材料”“采集”配置组：原生 `Pathing` 项目会自动挂载 `AutoPick`，JS 路线入口不会；现已在路线开始前显式启用同一 BetterGI 自动拾取触发器，并继续继承自动培养配置组的地图追踪/自动战斗参数，待实机验证。
  - 2026-07-20 已修正同一怪物路线因多个材料等级重复执行的问题；同一路线现在只执行一次，并把完整 3:1 材料链纳入最终统一复核。
  - 2026-08-02 已移除每条 JSON 后的背包读取和途中停止判断；本次选中的 JSON 会全部执行，最后统一计算总收益。该取舍避免频繁打断路线，但材料可能少量多刷；本地测试通过，待实机验证整次运行确实只有前后两轮背包复核。
  - [x] 2026-08-01 月莲 10 条路线实机获得 `49` 个；蕈兽共享路线只执行一次，并确认蕈兽孢子 `+4`、荧光孢粉 `+2`、孢囊晶尘 `+1`。
  - [x] 2026-08-01 错误采集队实机复测通过：只执行“首次切换＋神像重试”两次，失败后不再对 10 条路径循环切队，并继续切换怪物队执行蕈兽路线。
  - 已匹配路线材料会在本次计划中标记为自动路线来源并移出人工待办；路线未接入历史完成时间预估前仍不显示预计天数。
  - 待集中实机回归路线兼容性、切队、卡死/战斗失败和 OCR 差值。
- [x] 已验证并启用两条 BetterGI 秘境名称映射：
  - `精通秘境：炽炎祭场` → 太山府 → 「繁荣」；
  - `炼武秘境：雷云祭坛` → 震雷连山密宫 → 孤云寒林。
- [x] 周日奖励序号通用规则：周一/四组=第 1 项、周二/五组=第 2 项、周三/六组=第 3 项；映射可为例外显式覆盖。2026-08-01 计划模式确认昏识塔「巧思」传入 `2`、「笃行」传入 `3`；实际领奖按用户要求暂缓。
- [-] 已补全 42 条“培养材料来源关卡 → BetterGI `DomainName`”映射，覆盖 14 个材料秘境与 147 个材料条目。
  - 数据由 genshin-db 来源关卡与 BetterGI 内置 `tp.json` 奖励列表交叉对照生成；首批太山府「繁荣」已实机，其余待集中回归。
  - 回归失败时在 `codex/test-domain-mapping` 分支修正；不得用未经来源/奖励表对照的名称新增映射。
- [x] 规则库生成器使用 `data/bettergi-domain-catalog.json` 校验所有 `DomainName`，未知名称会在生成阶段失败。
- [x] 新增 `data/domain-validation-ledger.json`，为 42 条映射保存 BetterGI 版本、开放日、周日序号、测试材料、验证日期、结果与失败原因；40 条明确标记待验证，1 条历史实测、1 条目录核对不冒充完整验证。生成器与单元测试会拒绝缺项、错序号和失效映射。
- [x] `data/route-overrides.json` 支持单文件、文件数组或目录递归展开；校验 JSON 文件、路线类型和路径存在性，并允许为特殊路线指定专属队伍、所需角色说明或单独禁用。代码与模拟文件系统测试完成，实机暂缓。

## C. 秘境执行、树脂与安全

- [x] 构造 `AutoDomainParam` 并调用 `dispatcher.RunAutoDomainTask()`；支持队伍、战斗策略和指定树脂次数。
- [x] 秘境前切换指定队伍、传送七天神像恢复；切队失败会停止任务。
  - 统一顺序为“先传送七天神像，后切换队伍”，规避特殊地点禁止切队。
- [x] 不合成树脂；优先消耗已有浓缩树脂，再消耗原粹树脂。
  - 须臾、脆弱树脂默认关闭，须由用户显式开启。
- [-] 培养秘境单次测试：从级联运行模式选择后，强制只领取 1 次原粹树脂奖励，并禁用浓缩、须臾、脆弱树脂；本地测试通过，待后续实机验证。
- [x] 周日没有有效奖励序号时拒绝执行，防止领取错误材料。
- [-] 太山府已实机执行，确认不会再前往合成台，且会正确传入周日第 1 项。
- [x] 公开版不再依赖 BetterGI 任务返回的奖励字典：秘境、Boss 和圣遗物任务均关闭任务级奖励识别，整次运行只在开始前与全部任务结束后读取背包，收益以总差值确认。
  - 历史奖励字典解析器仅保留为开发诊断工具，不参与正式执行与统计；邮件会独立展示路线成功、失败或未确认状态。
  - 2026-07-20 实机发现执行后复查会偶发把已有高阶材料从 21 识别为 0；已增加同次运行库存不下降保护，待回归验证。
  - 2026-08-08 对照 BetterGI 接口和连续实机日志后，正式路径不再读取返回值；无背包增长证据时只写“任务调用结束，未确认是否成功领奖”，不再写成“已执行完成”。
- [-] 整次运行开始前读取一次目标材料和合成链，树脂任务与路线全部结束后再统一读取一次并记录总差值；中途不打开背包。待验证 OCR 对不同材料的稳定性。
- [x] 公开版新增顶层“我已确认配置并允许实际执行”复选框，默认不勾选；未确认时在读取背包、写文件、发送通知或操作游戏之前立即拒绝运行。原“仅生成计划”模式已移除。2026-08-09 已将该复选框移动到最上方“使用说明”之后，避免用户配置完全部选项后才看到执行确认。
- [x] 单次运行中根据开始时的缺口顺序调用多个树脂任务，并在全部任务结束后统一复核；不在多个秘境之间做精确比例分配。代码与模拟测试完成，实机暂缓。
- [x] 遇到未启用/不支持任务时按任务记录跳过并继续；配置、切队、传送、战斗等异常记录失败并停止后续树脂任务与路线。
- [x] 建立统一任务结果模型：`completed`、`skipped`、`failed`、`unconfirmed`，统一记录任务、原因、执行证据和是否允许继续后续任务。
- [x] 建立失败处理策略：安全错误停止后续树脂任务；条件性跳过继续下一个候选；路线缺失只影响对应路线；背包未确认不冒充成功。
- [x] 用模拟执行器覆盖“跳过后继续、多个调用后统一复核、树脂任务失败后停止、路线局部失败、背包未确认”场景，不进行游戏内实测。

## D. 调度、记录与通知

- [x] 同一秘境、同日、多阶材料合并为一次任务，避免重复进入。
- [x] 生成从今天起的七天循环策略，并在日志、计划记录和运行摘要中显示每天可刷的秘境。
- [x] 当前树脂任务排序：世界 Boss → 当日开放培养秘境（同类中限时项优先）→ 圣遗物填充；地方特产/怪物路线不耗树脂，不参与树脂排序。
  - 周本材料固定进入“手动获取”列表，不阻塞 Boss、培养秘境或路线；Boss 默认关闭，须由用户确认机制、专属队伍与战斗策略后显式开启。
  - 单次运行会按优先级依次调用已启用的树脂任务；跳过继续，安全失败停止；之后再执行路线。不在同次运行内精确分配多个秘境的树脂。
- [x] 配置页与正式运行日志写明当前实际规则：按固定任务顺序依次检查候选；Boss 只用原粹，培养/圣遗物秘境按所选树脂类型的 BetterGI 固定顺序消耗。
- [x] 保存 `record/latest-plan.json` 和最多 100 条的 `record/history.json`。
- [x] 完成日志按实际内容区分“树脂任务”“路线任务”和“未执行”，不再统一显示“至多一个已验证任务”。
- [-] 邮件/通知摘要：已调用 `notification.Send()`，已启用 `js.custom`；正文采用 HTML 分段显示本次状态、收益、缺口、候选和预计完成。
  - [x] 2026-08-04 至 08-07 连续邮件确认 HTML 分段与附件正常。
  - [x] 公开版 README 使用 `2739252596@qq.com` 作为问题与需求反馈邮箱；Bug 反馈提示附 BetterGI 版本、脚本版本、复现步骤和完整日志，并提醒发送前检查日志隐私。
  - [-] 2026-08-09 正式路径已关闭任务级奖励识别：摘要把“本次任务”和“背包确认收益”分开；圣遗物填充明确不纳入培养材料计数；背包无增长时不再声称执行成功。待一轮正式运行核对新文案。
- [x] 邮件摘要增加“当前养成状态”，显示角色当前等级、三项天赋当前等级和武器当前等级，并同时显示本次选择的目标等级。
  - [x] 2026-08-09 完成数据来源预研：现有手动/下拉目标已经保存角色、天赋和武器的当前值，近期版本可以直接使用，不需要额外进入角色界面；自动档案模式以后复用同一输出结构。
  - [x] 新增规范化 `profileSnapshot`：记录类型、名称、当前/目标等级、三项天赋当前/目标等级、武器归属、来源和读取时间；写入 `latest-plan.json` 与历史记录，并传给摘要生成器。
  - [x] 邮件在“本次状态”后优先显示完整条目，例如“申鹤 Lv.80→90｜天赋 6/8/8→9/9/9”“和璞鸢 Lv.80→90”；日志使用相同文案。
  - [x] 自定义角色目标未填写天赋时显示“天赋未提供（本次不计算）”，不得默认成 `1/1/1`；独立武器目标不伪造与角色的装备关系。
  - [x] 针对 BetterGI `notification.Send()` 的 500 字符硬限制重做摘要预算：当前状态优先，多目标仅按完整条目截断并显示“另 N 项见运行记录”，不产生残缺 HTML；默认仍只发送一封摘要。
  - [x] 单元测试覆盖单角色＋武器、角色无天赋、多角色/多武器、当前等于目标和超长安全截断。
  - [ ] 实机检查邮件排版、实际字符数和历史记录一致性；确认新增区块不会挤掉“本次状态”和关键失败原因。
- [-] 根据已确认背包差值建立历史均值，估算自动秘境材料的还需运行批次/天数；样本不足、收益为零或含未自动来源时明确显示原因。
  - [x] 路线材料按相同 JSON 文件集合的历史背包实际增量计算每轮均值和预计剩余轮次；至少需要 2 个正收益样本，路线集合变化、零收益或未确认记录不会沿用旧估算。路线刷新周期不纳入，仍不显示自然日。
  - 待用真实背包差值样本回归估算是否符合实际。
- [x] 在单次运行中按开始时的缺口顺序调用可执行树脂任务，直至候选结束或发生安全错误；树脂是否已耗尽由 BetterGI 各任务自行判断，实机验证暂缓。

## E. 待实机回归的树脂任务

- [-] 世界 Boss 执行器：已映射 39 项材料到 BetterGI 内置首领，复用 `RunAutoBossTask()`；只使用原粹树脂、支持专属 Boss 队伍/策略，失败会停止本次后续任务。
  - [x] 2026-07-20 急冻树单次实机通过：自动前往、自动战斗、领取一次奖励、奖励识别和背包前后差值均成功；极寒之核 `8 → 11`，确认收益 `×3`。空策略会复用 BetterGI 全局“自动首领”的战斗策略。
  - [-] 其余世界 Boss（含机制特殊的首领）仍待逐项回归，不得默认视为可稳定刷取。
  - [x] 增加“首领名称 → 专属队伍/策略/启用状态”覆盖；通用队伍仍作兜底，特殊 Boss 可单独禁用。配置解析和执行配置模拟测试完成，实机暂缓。
- [~] 周本自动执行不进入 0.9.0 公开版：37 项材料映射保留用于缺口计算与手动提示，执行器和设置项已移除。原因是机制、队伍、周次数限制及 30/60 树脂界面差异显著，尚不足以作为安全默认能力公开。
- [-] 圣遗物秘境：已实现为没有当天培养树脂任务时的可选填充任务；通过一个级联菜单同时选择关闭/单次测试/正式填充和目标秘境，支持独立队伍/策略，沿用普通秘境树脂规则，默认不自动分解圣遗物；待实机回归。

## F. 完成预估

- [-] 预计完成时间采用用户确认的世界等级 9 固定机制：世界 Boss 按 `3.1` 个/40 树脂；培养材料秘境按最高难度掉落期望；每日树脂预算默认 180。
  - 周本与圣遗物不显示预计天数；路线历史背包差值预估尚未接入，当前不显示预计天数；不考虑双倍掉落。
  - [x] 单元测试覆盖最高难度培养秘境、世界 Boss 和周本不预估规则。
  - [x] 2026-07-20 急冻树仅计划模式验证通过：极寒之核缺口 `15` 正确预估为 `5` 次领奖、约 `200` 树脂、约 `1` 天；日志、邮件与 `latest-plan.json` 一致，未消耗树脂。
  - [-] 2026-08-07 邮件改为同时显示“预计领奖次数、树脂、所需开放日、从当前日期起的最早自然日”，避免周三/周四运行时自然日数字变化被误解为材料进度倒退；待下一封真实邮件检查长度与排版。

## G. 自动读取当前档案并生成目标（预开发）

- [x] 确认目标输入边界：用户仍须选择角色目标等级、普通攻击/元素战技/元素爆发各自的目标等级和自动适配武器的目标等级；只自动读取当前值与佩戴武器，不自动决定培养上限。
- [x] 2026-08-09 核对 BetterGI 最新正式发布版 `0.63.0` 源码：JS 全局对象为 `characterDevelopmentTask`，公开 `GetCharacter(name, categories)` 与 `GetMultiCharacters(names, categories)`；分类使用“属性;武器;天赋”。返回字段为角色等级/上限、武器名称与等级/上限、三项天赋显示等级及各自的命座加成标记。官方同时警告 0.63.0 识图底层重构后可能存在未知 BUG，因此只据其接口开发代码，实测暂缓，当前安装版不升级。
- [x] 增加版本/能力检测：优先用 `typeof characterDevelopmentTask !== 'undefined'` 和方法存在性判断能力，不只比较版本字符串；0.62.0 继续支持手动模式，自动模式缺少接口时给出中文错误。
- [x] 新增 BetterGI 0.63.0 档案适配器：调用 `GetCharacter`/`GetMultiCharacters`，兼容 PascalCase/camelCase 字段并归一化为项目内部结构；请求分类固定为“属性;武器;天赋”。
- [x] 将天赋界面等级与实际培养等级分开保存；对应 `HasBonus=true` 时按命座 `+3` 转换实际消耗等级，并校验转换结果在合法范围内。
- [x] 初始武器按 `rarity=1` 自动忽略，并保留五把已知一星武器的兼容兜底。
- [x] 增加角色档案数据结构与来源抽象；BetterGI 0.63.0 为自动模式来源，现有手动目标作为兼容来源。
- [x] 将识别结果转换为现有目标格式，并在计划日志/邮件中同时显示当前状态和最终培养目标；识别失败时在背包扫描前拒绝调度。
- [x] 自动档案结果与手动目标共用档案快照和邮件区块；命座 `+3` 保留界面与实际等级，一星初始武器显示“已忽略培养”。
- [x] 增加“手动档案/自动档案”模式配置。自动模式只要求角色、目标等级、三项天赋目标和佩戴武器目标等级。
- [x] 使用模拟的 0.63.0 返回对象完成单元测试：单/多角色、命座加成、满级角色、普通/一星/满级武器、旅行者、未知名称、空字段、接口不存在和接口抛错。
- [~] 游戏内档案识别实测暂缓。实测开始前提醒用户补充此前尝试过的界面识别方案及结果；届时覆盖零命/命座角色、旅行者、新角色、不同武器和识别失败恢复。

## H. 树脂优先级与规则自定义（预研）

- [ ] 将“任务类型优先级”与“树脂类型优先级”建模为两个独立配置，避免把先刷什么和使用哪种树脂混为一谈。
- [ ] 设计任务优先级预设与自定义排序：世界 Boss、限时培养秘境、普通培养秘境、圣遗物填充；地方特产/怪物路线作为不耗树脂任务单独设置在树脂任务前或后。周本在重新完成独立安全评审前不加入可选排序。
- [ ] 设计原粹树脂保留量、每类任务最大次数/树脂上限、圣遗物填充条件和须臾/脆弱树脂单独授权；默认值必须保持当前安全行为。
- [ ] 研究按运行开始时的材料缺口限制最大领取次数；保持“整次运行只读前后两次背包”，不为追求刚好补齐而在每次领奖后打开背包。内部奖励识别不稳定时必须明确这是预算上限而非精确完成判断。
- [ ] 明确不可由自定义配置绕过的硬约束：开放日、真实材料缺口、执行确认、队伍必填、已验证映射、周日奖励序号、Boss 不使用浓缩树脂。
- [ ] 设计中文可读且可防错的配置格式；保存前拒绝重复类型、未知类型、非法顺序、负数上限和互相冲突的规则。
- [ ] 在不执行游戏操作的独立配置校验工具中输出最终排序、每项消耗上限、树脂保留量及每个任务的选择/跳过原因，再进入实机回归。
- [ ] 自定义优先级只控制顺序和上限，不实现多个秘境之间的精确比例分配。

## I. 开发环境稳定性

- [x] 统一使用项目已可用的 pnpm，`package.json` 锁定 pnpm 11.9.0 与 Node.js 22+；不再调用会跳转到沙箱不可读用户全局目录的 npm。
- [x] `genshin-db` 固定从项目本地 `node_modules` 解析；项目级 `AGENTS.md` 禁止假定 Codex 公共运行时已经安装该依赖。
- [x] 增加项目级 `.gitattributes`，统一文本文件为 LF，并显式标记常见二进制文件。
- [x] 仓库本地 Git 配置已改为使用项目 `.gitignore`，`git status` 不再访问沙箱不可读的默认全局 ignore 路径，也不再产生权限警告。
- [x] 项目代理约定普通自动化命令不加载用户 PowerShell profile，避免非交互输出触发无关的 PSReadLine 预测提示警告。

## 下一步顺序

1. 实现邮件“当前养成状态”和统一档案快照，先使用现有手动目标中的当前值，不等待游戏界面自动识别。
2. 基于 BetterGI 0.63.0 实现能力检测、角色档案适配器、命座等级转换和模拟接口测试；不升级当前安装版、不进行游戏识别。
3. 开发自动档案配置界面和“识别结果 → 现有目标”转换，保留 0.62.0 手动模式兼容。
4. 建立统一失败状态模型与模拟执行测试，再实现按优先级依次处理多个树脂任务；不开发多个秘境之间的精确比例分配。
5. 建立秘境映射验证档案格式和生成校验；实际秘境领奖、周日第 2/3 项、Boss、路线、圣遗物和背包 OCR 回归全部暂缓。
6. 接入路线历史背包差值的收益均值与预计轮次模型，只用模拟历史数据验证，真实样本以后补充。
7. 完成路线目录覆盖、Boss 专属配置和树脂优先级/规则自定义的本地实现与配置预览。
